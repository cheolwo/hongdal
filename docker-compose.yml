services:
  app:
    build:
      context: .
      dockerfile: Hongdal/Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "${HONGDAL_ASPNETCORE_ENVIRONMENT:-Development}"
      ASPNETCORE_URLS: http://+:8080
      DOTNET_RUNNING_IN_CONTAINER: "true"
      ConnectionStrings__DefaultConnection: "${HONGDAL_DB_CONNECTION_STRING:-server=mysql;port=3306;database=hongdal_dev;user=hongdal;password=YOUR_LOCAL_PASSWORD;}"
      Redis__ConnectionString: "${HONGDAL_REDIS_CONNECTION_STRING:-redis:6379}"
      MongoDb__ConnectionString: "${HONGDAL_MONGO_CONNECTION_STRING:-mongodb://hongdal:YOUR_LOCAL_PASSWORD@mongo:27017/?authSource=admin}"
      MongoDb__Database: hongdal_dev
      DatabaseInitialization__RunAtStartup: "${HONGDAL_DATABASE_INITIALIZATION_AT_STARTUP:-false}"
      DatabaseInitialization__FailOnError: "true"
      Jwt__Issuer: Hongdal.Dev
      Jwt__Audience: Hongdal.Client.Dev
      Jwt__SecretKey: "${HONGDAL_JWT_SECRET_KEY:-development-only-jwt-secret-key-change-me-32bytes}"
      IsmsPProtectedData__Aes256GcmKeyBase64: "${HONGDAL_ISMSP_AES256_GCM_KEY_BASE64:-}"
      IsmsPProtectedData__HashSalt: "${HONGDAL_ISMSP_HASH_SALT:-}"
      IsmsPProtectedData__TransportKeyId: "${HONGDAL_ISMSP_TRANSPORT_KEY_ID:-hongdal-local-transport-v1}"
      IsmsPProtectedData__TransportPublicKeyPem: "${HONGDAL_ISMSP_TRANSPORT_PUBLIC_KEY_PEM:-}"
      IsmsPProtectedData__TransportPrivateKeyPem: "${HONGDAL_ISMSP_TRANSPORT_PRIVATE_KEY_PEM:-}"
      IsmsPProtectedData__FailWhenKeyMissing: "true"
      AgriculturalFisheriesBatch__Enabled: "${HONGDAL_AGRICULTURAL_FISHERIES_BATCH_ENABLED:-false}"
      AgriculturalFisheriesBatch__KamisDailyEnabled: "${HONGDAL_KAMIS_DAILY_BATCH_ENABLED:-true}"
      AgriculturalFisheriesBatch__KamisMonthlyEnabled: "${HONGDAL_KAMIS_MONTHLY_BATCH_ENABLED:-true}"
      AgriculturalFisheriesBatch__UsdaMonthlyEnabled: "${HONGDAL_USDA_MONTHLY_BATCH_ENABLED:-true}"
      PublicData__Kamis__CertificationKey: "${HONGDAL_KAMIS_CERTIFICATION_KEY:-}"
      PublicData__Kamis__RequesterId: "${HONGDAL_KAMIS_REQUESTER_ID:-}"
      PublicData__UsdaNassQuickStats__ApiKey: "${HONGDAL_USDA_NASS_API_KEY:-}"
      TossPayments__ClientKey: "${HONGDAL_TOSS_CLIENT_KEY:-development-only-client-key}"
      TossPayments__SecretKey: "${HONGDAL_TOSS_SECRET_KEY:-development-only-secret-key}"
      GoogleGeocodingApiKey: "${HONGDAL_GOOGLE_GEOCODING_API_KEY:-development-only-google-geocoding-key}"
      NaverCloudDirections__ApiKeyId: "${HONGDAL_NAVER_CLOUD_DIRECTIONS_API_KEY_ID:-development-only-naver-key-id}"
      NaverCloudDirections__ApiKey: "${HONGDAL_NAVER_CLOUD_DIRECTIONS_API_KEY:-development-only-naver-key}"
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "--show-error", "http://127.0.0.1:8080/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongo:
        condition: service_healthy

  mysql:
    image: mysql:8.4
    restart: unless-stopped
    ports:
      - "13306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "${HONGDAL_MYSQL_ROOT_PASSWORD:-YOUR_LOCAL_ROOT_PASSWORD}"
      MYSQL_DATABASE: hongdal_dev
      MYSQL_USER: hongdal
      MYSQL_PASSWORD: "${HONGDAL_MYSQL_PASSWORD:-YOUR_LOCAL_PASSWORD}"
      TZ: Asia/Seoul
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p${HONGDAL_MYSQL_ROOT_PASSWORD:-YOUR_LOCAL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    ports:
      - "16379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - redis_data:/data

  mongo:
    image: mongo:8.0
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: hongdal
      MONGO_INITDB_ROOT_PASSWORD: "${HONGDAL_MONGO_ROOT_PASSWORD:-YOUR_LOCAL_PASSWORD}"
      MONGO_INITDB_DATABASE: hongdal_dev
    healthcheck:
      test: ["CMD-SHELL", "mongosh --quiet --eval 'db.runCommand({ ping: 1 }).ok' | grep 1"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - mongo_data:/data/db

volumes:
  mysql_data:
  redis_data:
  mongo_data:
