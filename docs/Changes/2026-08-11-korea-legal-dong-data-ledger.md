# 2026-08-11 대한민국 법정동 데이터 원장

## 변경

- 행정표준코드관리시스템의 법정동코드 전체자료 ZIP을 명시적 활성화에서만 수집한다.
- 원본 ZIP은 private object storage에 보관하고 DB에는 hash·크기·보관 위치 metadata를 남긴다.
- 10자리 법정동 code, 전체명, 존재·폐지, 계층과 상위 code를 정규화한다.
- 현행 code만 `region:kr:bjd:{code}` 행정구역과 `KR-MOIS-ADMIN` 공식 code 근거로 승격한다.
- 세종특별자치시 `3611000000`은 공식 현행 전체자료의 최상위 구역으로 처리한다.
- 폐지일이 제공되지 않는 행을 임의의 유효종료일로 만들지 않는다.

## 실제 로컬 저장 결과

| 항목 | 결과 |
| --- | ---: |
| 비공개 원본 ZIP | 1개, 413,346 bytes |
| 원본 SHA-256 앞 20자리 | `7b4b544a6302d26c4f4c` |
| 정규화 record | 53,387건 |
| 현행 법정동 | 20,560건 |
| 지도 행정구역 원장 | 20,560건 |
| 공식 code assignment | 20,560건 |
| 미확인 상위 구역 | 0건 |

로컬 원본은 `Ssalddel/.local-private-storage/external-data/raw/` 아래에 있으며 Git에는 포함하지 않는다.

## 대표 좌표 경계

공식 대표 좌표는 국토교통부·국가공간정보센터의 전국 법정구역 SHP 경계에서 내부점을 계산하는 것을 기준으로 한다. 공공데이터포털에서 공식 자료와 최신 전체자료 기준일 `2026-07-01`을 확인했으나 VWorld 다운로드가 로그인 세션을 요구해 이번 실행에서는 원본을 취득하지 못했다.

따라서 시청·구청 위치, 이름 기반 geocoding, 임의 중심점으로 경계 원장을 채우지 않았다. 현재 `regional_agricultural_map_region_boundaries`의 이번 법정동 대표 좌표는 0건이며, 공식 SHP 원본을 확보한 뒤 좌표계 변환·polygon 내부점 계산·원본 hash 연결을 별도 수집 단위로 구현한다.

## 검증

- 대한민국 법정동 집중 test 9/9 통과
- 실제 공식 ZIP 수집·중복 hash 재처리·DB 재조회 완료
- Interpretation, Simulation, Unity 변경 없음
