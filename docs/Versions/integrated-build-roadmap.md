# 통합 구축 로드맵

이 문서는 이미 존재하는 여러 버전과 역할별 앱을 한 제품으로 확장하는 실제 개발 순서를 정한다. `0.0`은 공개 운영 기반이고, `1.0` 이후 버전은 의존성·법률·운영 경계로 유지한다. 구현 순서는 버전 번호를 직렬로 완료하는 방식이 아니라 아홉 워크플로우의 최소 작동 루프를 차례로 닫는 방식으로 정한다.

## 두 개의 독립 축

| 축 | 판단 기준 |
| --- | --- |
| 제품 버전 | 기능이 처음 도입된 시점, 선행 의존성, 릴리즈 게이트 |
| 페이지 capability | 현재 환경에서 조회·저장·상태 전이·외부 실행 중 어디까지 가능한지 |

기능 플래그는 페이지와 API의 노출 가능성을 정한다. 실제 외부 효과는 전역 `SsalddelExecution:Mode`가 정한다. 기능 플래그가 켜져 있어도 `Simulation`에서는 자동 배차, 계약 확정, 실결제, 실정산, 실제 보관과 외부 신고를 실행하지 않는다.

## 공통 완성 단계

| 단계 | 최소 조건 |
| --- | --- |
| 준비 중 | 라우트와 책임은 식별되지만 실제 업무 입력을 받지 않는다. |
| 체험 | 교체 가능한 샘플 또는 세션 데이터로 화면과 연결 구조를 확인한다. |
| 베타 | Controller API를 통해 조회·저장하고, 새로고침과 서버 재시작 뒤에도 같은 원장을 읽는다. 인증, 빈 상태, 오류, 재시도를 제공한다. |
| 운영 | 권한, 개인정보, 감사, 복구, 멱등성, 브라우저 E2E와 운영 절차를 통과한다. 외부 효과는 별도 승인과 `Operational` 모드가 필요하다. |

단계 표시는 임의의 화면 문구가 아니라 `SsalddelPageCapabilityCatalog`와 `GET /api/v1/version-feature-flags`의 `PageCapabilities`를 기준으로 한다.

## 우선순위

### P0. 페이지 기준 원장

다른 기능을 넓히기 전에 화면과 운영 경계를 추적할 기반을 만든다.

1. 전체 Razor 페이지와 `@page` 라우트를 앱별로 자동 발견한다.
2. `(AppCode, RouteTemplate)`을 페이지 소유 식별자로 사용하고 중복 소유를 테스트한다.
3. 대표 라우트 계열에 단계, 도입 버전, 워크플로우, 기능 플래그, 인증, 외부 효과 여부를 부여한다.
4. 통합 WebApp과 서버 메타데이터가 같은 capability catalog를 사용한다.
5. 관리자 검토 상태를 프로세스 메모리가 아닌 서버 영속 저장소로 옮긴다.
6. 전문 앱의 모든 라우트를 같은 catalog로 확장한다.

현재 기준선은 10개 UI 프로젝트, 233개 Razor 페이지 파일, 260개 라우트다. 이 수치는 기능을 제한하는 상한이 아니라 의도하지 않은 누락을 발견하는 회귀 기준이다.

### P1. 각 워크플로우의 실제 조회

각 워크플로우에서 대표 허브·목록·상세 하나를 샘플 fallback 없이 서버에 연결한다.

1. 커뮤니티 신뢰: 게시판 → 글 상세 → 댓글
2. 국내 화물 운송: 운송 의뢰 상세 → 화주·관리자 동일 ID 조회
3. 창고 입출고: 입고 예정 → 작업 보드
4. 공동주문 수입: 모집·참여 의향 → 공동 원장
5. 통관·무역 데이터: HS 검토 목록 → 검토 상세
6. 판매채널 출고: 채널·주문 목록 → 출고 후보
7. 음식 배달: 음식점·메뉴 → 주문 상세
8. 알뜰살뜰 마트: 상품·주문 → 피킹 작업
9. 참여 인력 관리: 역할 지원 → 검토 목록

### P2. 각 워크플로우의 영속 작성

P1과 같은 순서로 업무마다 작성 하나를 완성한다. 서버 검증, 멱등성, 저장 후 재조회, 실패 시 샘플 성공으로 위장하지 않는 것이 공통 조건이다.

### P3. 서버 검증 상태 전이

업무마다 한 개의 상태 전이를 완성한다. 클라이언트는 서버 성공 응답 뒤 원장을 다시 조회한다. 상태 변경 Event, 감사와 필요한 Outbox를 함께 검증한다.

### P4. 워크플로우 인계

1. 커뮤니티 화물 글 → 운송 의뢰 초안
2. 공동구매 원장 → 창고 또는 운송 인계
3. 판매 주문 → 창고 출고 → 운송 인계
4. 음식·마트 준비 완료 → 기사 배송 Simulation
5. 완료 활동 → 공개 동의를 받은 커뮤니티 신뢰 신호

자동 전환은 하지 않는다. 관심, 참여, 연락처 공개, 원장 전환과 실행은 각각 명시적인 동의 상태를 가진다.

### P5. 운영 강화

권한, 개인정보 최소화, 속도 제한, 감사, 백업·복구, Outbox 재처리, 모바일·데스크톱 검증을 완료한다. 외부 결제·계약·배차·정산은 관련 릴리즈 게이트와 별도 운영 승인을 통과할 때까지 비활성으로 유지한다.

## 작업 방식

- 동시에 진행하는 세로 흐름은 하나로 제한한다.
- 한 흐름은 `Contract → Controller API → UseCase/Command → DB/Event → 공용 UI → 브라우저 검증`까지 닫는다.
- 한 워크플로우를 운영 단계까지 몰아서 개발하지 않고, P1 조회를 전체에 한 바퀴 적용한 뒤 P2 작성으로 이동한다.
- `Ssalddel.WebApp`은 통합 브라우저 셸로 사용하고 공용 화면·ViewModel은 `Ssalddel.Ui.Common`에 둔다. 역할별 앱은 삭제하지 않고 장치·역할 adapter로 유지한다.
- 검증 환경에서는 `ClientDataMode.AllowSampleFallback=false`로 실제 서버 실패가 드러나게 한다.

## 바로 이어갈 세로 흐름

첫 통합 인계는 `커뮤니티 화물 글 → 운송 의뢰 초안으로 가져오기 → /shipper/request/{id} 재조회 → 관리자 /requests에서 같은 ID 확인` 순서로 닫는다. 자동 배차나 계약 없이도 커뮤니티와 1.0 업무를 연결할 수 있어 첫 통합 인계로 사용한다.

2026-07-20 기준으로 첫 단계인 `커뮤니티 화물 글 → /shipper/request 초안`을 완료했다. 사용자가 화물 글 상세에서 직접 가져오기를 선택해야 하며, 제목·본문만 비파괴적으로 합치고 주소·연락처·수량·운임은 자동 추출하지 않는다. 초안은 새로고침 뒤 복구되고 같은 글을 중복 반영하지 않는다. `DomesticTransportWorkflow`가 비활성인 환경에서는 서버 등록 버튼을 잠그고 초안 작성·저장만 허용한다.

2026-07-20 기준으로 두 번째 단계인 `/shipper/request/{id}` 재조회를 완료했다. 서버 등록이 성공하면 생성 응답의 같은 의뢰 ID 상세로 이동해 다시 조회하고, 직접 ID를 입력하는 `/shipper/request/detail` 진입점도 제공한다. 예시 ID fallback은 제거했으며 로그인, 소유자·관리자 조회 권한, 기능 플래그 비활성, 조회 실패와 재시도 상태를 화면에 분리했다. 상세 조회만으로 자동 배차·계약 확정·실결제가 실행되지 않는다는 경계도 표시한다.

2026-07-20 기준으로 세 번째 단계인 관리자 `/requests` 목록 연결을 완료했다. 서버관리자 권한과 `DomesticTransportWorkflow`를 확인한 뒤 최근 의뢰를 조회하고, 의뢰 ID 부분 검색과 결제·배차 상태 조건, 오류·재시도·빈 상태를 제공한다. 목록의 상·하차지는 전체 도로명·번지 대신 권역만 표시하며, 같은 의뢰 ID의 관리자 상세와 관제 화면으로 이동한다. 개발 메모리 데이터는 명시적인 샘플 경계로 구분한다.

2026-07-20 기준으로 네 번째 단계인 관리자 `/requests/{RequestId}` 상세 보완을 완료했다. 서버관리자 권한과 `DomesticTransportWorkflow`를 상세 진입 시 다시 확인하고, 기본 화면에는 상·하차 권역만 표시하며 상세 주소와 정밀 좌표는 관리자가 업무상 필요할 때 직접 펼쳐 확인한다. 취소·환불은 운송 시작 전 상태에서만 허용하고 사유와 의뢰 ID 재입력을 클라이언트와 서버에서 모두 검증한다. 서버 성공 뒤 같은 의뢰를 다시 조회하며, `Simulation`에서 의뢰·결제·정산·배차 투영과 감사 이벤트를 한 번에 기록한다. 외부 PG를 호출하지 않으며 `Operational` 모드에서는 실제 환불 연동이 준비되기 전 실행을 거부한다.

이로써 첫 통합 인계인 `커뮤니티 화물 글 → 운송 의뢰 초안 → 화주 동일 ID 재조회 → 관리자 목록·상세 확인`을 닫았다.

2026-07-20 기준으로 P1 창고 흐름의 첫 페이지인 창고관리자 `/warehouse/inbounds/expected` 입고 예정 조회를 완료했다. 토큰 세션 저장·만료·갱신, 로그인 API, 창고 역할 판단, 페이지 capability 확인, 입고 예정 조건 고정과 표시 문구를 각각 분리하고 PageViewModel은 인증 후 창고 조회·선택·목록 갱신만 조정한다. 샘플 fallback 없이 보호 API의 서버 페이징 목록을 사용하며, 기능 비활성·로그인·역할 거부·로딩·창고 없음·목록 없음·오류·재시도 상태를 제공한다. 행을 선택하면 같은 `inboundId`를 `/work-board?inboundId={id}`로 전달한다. 개발용 `shipper1`에는 기존 보안 역할과 일치하는 전역 `Warehouse.Manager` HR 세부 역할을 멱등 시드한다. 이 페이지는 capability catalog에서 인증이 필요한 `Beta/ReadOnly`로 기록하며, 조회만으로 실제 보관·계약·정산 효과는 발생하지 않는다.

2026-07-20 기준으로 P1 창고 흐름의 두 번째 페이지인 창고관리자 `/work-board`를 완료했다. `inboundId` query를 받아 `GET /api/v1/warehouse-operations/inbounds/{inboundId}`로 같은 입고 요청을 다시 조회하며, 서버는 요청자·창고 소유자·배정 사용자 범위 밖의 ID를 존재하지 않는 ID와 동일하게 404로 처리한다. 정적 작업 카드와 샘플 피킹 조회를 제거하고 페이지 조정, 단건 조회, 서버 상태 해석을 각각 PageViewModel·공용 상세 ViewModel·순수 정책으로 분리했다. 기능 비활성·로그인·HR 역할 거부·선택 없음·로딩·대상 없음·API 오류·재시도 상태를 제공하고, 입고 예정·운송 중·완료·취소 상태별 다음 행동을 안내한다. 이 페이지는 인증이 필요한 `Beta/ReadOnly` capability이며 검수·적재·실제 보관·정산 상태 전이는 실행하지 않는다.

이로써 P1 창고 입출고의 `입고 예정 → 같은 ID 작업 보드 재조회` 흐름을 닫았다.

2026-07-20 기준으로 P1 공동주문 흐름의 첫 페이지인 통합 웹 `/community/group-purchase` 공개 조회를 완료했다. 보호된 주문자 API에 섞여 있던 모집 읽기를 0.0 커뮤니티 공개 투표 API로 분리하고, 목록 ViewModel은 국내 공동구매 필터와 정렬만, 상세 ViewModel은 명시적인 `campaignId`와 연결 댓글 조회만 담당한다. 주소의 ID가 없을 때만 첫 모집을 선택하며, 명시한 ID가 없거나 공동수입인 경우 다른 항목으로 대체하지 않는다. 익명 공개 조회와 로그인 후 제안·참여 저장 경계를 안내하고 로딩·빈 목록·상세 없음·API 오류·재시도를 구분한다. 저장 명령 뒤에는 보호 API 응답을 화면에만 덮지 않고 같은 공개 ID를 다시 조회한다. 이 페이지는 익명 진입이 가능한 `Beta/Simulation` capability이며 참여 의향은 결제·계약·모집 마감·물류 실행을 자동 확정하지 않는다.

2026-07-20 기준으로 P1 통관·무역 데이터의 통합 웹 `/shipper/customs/hs-reviews`를 완료했다. 화면 내부의 `CapabilitySamples`, `Reviews`와 지연 기반 샘플 생성을 제거하고, 화주·판매자 정책과 `CustomsAndTradeDataWorkflow`로 보호된 목록·상세 Controller API를 추가했다. 목록, 정확한 `reviewId` 상세, 로그인·기능 플래그 접근 판정과 페이지 조정을 각각 독립 ViewModel로 분리했다. 활성 HS 코드와 활성 원장 버전만 조회하며 공식 공개 분류사례, 제공자가 공개에 동의한 무료 대행 경험만 반환한다. 비공개 사례, 비동의·유료 경험과 제공자 식별자는 응답 계약에서 제외했다. 출처·개정판·적용 기준일·원장 반영 시각·분류 단위를 표시하고, 기능 비활성·로그인 필요·목록 없음·상세 없음·권한 오류·재시도를 구분한다. 이 페이지는 인증이 필요한 `Beta/ReadOnly` capability이며 품목분류·세율·세관 신고·자동 배차·계약을 확정하지 않는다.

2026-07-20 기준으로 P1 판매채널 출고의 첫 페이지인 통합 웹 `/shipper/sales/channels`를 완료했다. WebApp 프로세스 메모리의 기본 계정과 인증 메모 입력을 제거하고, 로그인 사용자 소유 목록과 정확한 `accountId` 상세를 기능 플래그로 보호된 Controller API에서 조회한다. 서버는 다른 사용자의 ID를 존재하지 않는 ID와 같은 404로 처리하고 서버관리자만 운영 검토 범위에서 전체 원장을 볼 수 있다. 기능·로그인·역할 접근 판정, 목록·검색 필터, 단건 상세, 연결 준비 저장을 각각 독립 ViewModel로 분리했으며, 선택 전 첫 계정이나 샘플 계정을 표시하지 않는다. 채널과 상점명만 플랫폼 내부 준비 원장에 저장하고 외부 비밀번호·API key·access token 입력은 받지 않는다. 기능 비활성·로그인 필요·역할 거부·원장 없음·필터 결과 없음·상세 없음·401·403·API 오류·재시도를 구분한다. 이 페이지는 인증이 필요한 `Beta/PlatformPersistence` capability이며 외부 OAuth, 상품 발행, 주문 수집·동기화를 실행하지 않는다.

2026-07-20 기준으로 P1 판매채널 출고의 두 번째 페이지인 통합 웹 `/shipper/sales/orders`를 완료했다. 페이지 내부에서 만들던 샘플 주문·피킹·알림 상태를 제거하고, 외부 채널 주문 중 상품 매핑과 재고 예약에 성공해 `출고예정`으로 영속된 판매채널 출고 후보만 읽는다. 현재 구조에는 수집한 외부 주문 원본과 실패 주문을 보존하는 별도 원장이 없으므로 이를 전체 주문 목록이나 재고 부족 주문처럼 표현하지 않는다. 서버는 판매자 소유 범위를 적용하고, 관리자 조회에서도 같은 주문참조번호를 판매자별로 분리해 묶는다. 목록·검색·국내외·출고 상태·서버 페이징, 정확한 `orderId` 상세와 원장 없음·필터 결과 없음·상세 없음·오류·재시도를 제공한다. 출고 라인에는 상품·SKU·수량·창고명과 운송 인계 여부만 반환하며 구매자·수령자 식별자와 창고 주소는 계약에서 제외했다. 기능·로그인·역할 접근, 목록, 상세와 페이지 조정을 각각 독립 ViewModel과 읽기 service로 분리했다. 이 페이지는 인증이 필요한 `Beta/ReadOnly` capability이며 진입·새로고침으로 외부 주문 동기화, 피킹·포장·출고·운송 상태 전이를 실행하지 않는다.

이로써 P1 판매채널 출고의 `채널 원장 → 영속된 주문 출고 후보 재조회` 흐름을 닫았다.

2026-07-20 기준으로 P1 음식 배달의 첫 페이지인 주문자 앱 `/food/restaurants`를 완료했다. 페이지 내부의 정적 음식점 목록과 클라이언트 샘플 정책 fallback을 제거하고, 공개 음식점 프로필과 공개 메뉴를 서버 RDB 원장으로 분리했다. 정산 조건·연락처·운영 메모를 가진 `업체`와 거래 이력인 `음식주문`을 공개 디렉터리 원장으로 재사용하지 않는다. 목록은 사용자가 직접 고른 공개 행정권역 기준점·반경·검색어·주문 가능 여부로 서버 페이징 조회하고, 상세는 명시적인 `restaurantId`의 공개 메뉴만 반환하며 없거나 비공개인 ID를 다른 음식점으로 대체하지 않는다. 현재 위치와 주소는 자동 수집하지 않고 기준점과 직선거리의 출처를 표시한다. 기능 접근, 탐색 기준, 목록, 정확한 상세와 페이지 조정을 각각 독립 service·ViewModel·공용 component로 분리했다. 기능 비활성·정책/권역 오류·선택 전·원장 없음·필터 결과 없음·상세 없음·재시도를 구분한다. 이 페이지는 익명 진입이 가능한 `Beta/ReadOnly` capability이며 조회로 주문·결제·조리 접수·배차를 생성하지 않는다. 운영자가 공개 프로필과 메뉴를 채우기 전에는 샘플 대신 빈 상태를 그대로 표시한다.

2026-07-20 기준으로 P1 음식 배달의 두 번째 페이지인 주문자 앱 `/orders`를 완료했다. 음식점·마트·화물 주문을 섞어 표시하던 하드코딩 카드를 제거하고, 로그인한 주문자가 소유한 영속 `음식주문` 목록과 명시적인 `orderNo` 상세만 기존 음식 주문 경로에서 조회한다. 서버는 목록과 상세 모두 JWT 사용자 ID를 원장의 `주문자UserId`와 비교하고, 다른 사용자의 주문번호를 존재하지 않는 주문과 동일한 404로 처리한다. 목록 계약에는 주문번호·음식점·상품 요약·금액·상태만 포함하고, 수령지·연락처·결제 정보는 정확한 주문 상세의 접힌 개인정보 영역에서만 표시한다. 주문자 ID, 음식점 정밀 좌표, 배차 내부 ID와 공동 원장 내부 ID는 화면 계약에서 제외했다. 기존 음식 주문 접수 UseCase에서 읽기 Query 책임을 제거하고 소유권 조회 UseCase, 인증 adapter, 보호 API client, 인증·목록·상세 ViewModel과 공용 component로 분리했다. MAUI 보안 저장소의 세션 복원·갱신·로그아웃을 제공하고, 기능 비활성·로그인 필요·원장 없음·필터 결과 없음·상세 없음·401·API 오류·재시도를 구분한다. 첫 주문이나 샘플 주문은 자동 선택하지 않는다. 이 페이지는 인증이 필요한 `Beta/ReadOnly` capability이며 조회로 주문 생성·음식점 수락·결제 승인·배차 상태 전이를 실행하지 않는다.

이로써 P1 음식 배달의 `공개 음식점·메뉴 → 로그인 주문자 소유 음식 주문 목록·정확한 상세` 조회 흐름을 닫았다.

2026-07-20 기준으로 P1 알뜰살뜰 마트의 첫 페이지인 주문자 앱 `/food/mart`를 완료했다. 정적 상품 네 건과 동작하지 않는 담기 버튼을 제거하고, 판매자·창고 위치·계약·정산 정보가 있는 `판매상품`·`입고상품` 및 거래 이력인 `마트주문`과 분리된 `마트공개상품` 투영을 추가했다. 공개 여부, 판매 허용 여부, 판매 가능 수량과 재고 기준 시각만 익명 Controller API로 조회하며 목록 검색·판매 가능 필터·서버 페이징과 정확한 `productId` 상세를 제공한다. 비공개·삭제된 ID를 첫 상품이나 샘플로 대체하지 않고 기능 비활성·로딩·원장 없음·필터 결과 없음·상세 없음·API 오류·재시도를 구분한다. 기능 접근, 목록, 상세와 페이지 조정을 각각 독립 service·ViewModel·공용 component로 분리했다. 이 페이지는 익명 진입이 가능한 `Beta/ReadOnly` capability이며 조회로 장바구니·주문·재고 예약·피킹·포장·기사 픽업을 실행하지 않는다.

2026-07-20 기준으로 P1 알뜰살뜰 마트의 두 번째 페이지인 창고관리자 앱 `/mart/picking`을 완료했다. 공용 컴포넌트가 보유하던 샘플 주문, 샘플 초기화, 피킹 수량 증가, 피킹·포장·출고 완료의 클라이언트 상태 전이를 제거했다. 서버는 로그인 사용자가 직접 담당한 작업, 상대 작업자로 연결된 작업, 소유하거나 배정된 창고의 작업만 조회하고 서버관리자만 전체 운영 범위를 확인한다. 목록은 검색·작업 상태·창고 범위·서버 페이징을 지원하고, 명시적인 `orderId` 상세는 접근 불가 주문을 존재하지 않는 주문과 같은 404로 처리한다. 응답에는 구매자·판매자 사용자 ID, 주소, 연락처, 창고 주소와 공동 원장 내부 ID를 포함하지 않고 주문 상품, 작업 유형·상태·위치와 작업자 표시명만 반환한다. 기능 접근, 로그인, 목록, 정확한 상세와 페이지 조정을 각각 독립 UseCase·client·ViewModel·공용 component로 분리했다. 기능 비활성·로그인 필요·원장 없음·필터 결과 없음·상세 없음·401·403·API 오류·재시도를 구분한다. WarehouseManagerApp과 같은 공용 화면을 쓰는 통합 WebApp의 `/warehouse/mart/picking`, `/mart/picking` 호환 경로도 인증된 `Beta/ReadOnly` capability로 맞췄다. 피킹 시작·수량 확정·포장 완료·기사 인계는 P3 서버 상태 전이 전까지 실행하지 않는다.

이로써 P1 알뜰살뜰 마트의 `공개 상품 투영 조회 → 접근 가능한 영속 주문·피킹 작업 조회` 기준선을 닫았다. 상품에서 주문을 생성하는 작성 흐름은 P2에서 별도 멱등 Command로 연결한다.

2026-07-20 기준으로 P1 참여 인력 관리의 HumanResourcesManagerApp `/` 역할 검토 조회를 완료했다. 정적 `역할 미확정` 지표, 작업 큐와 참여 혜택 샘플을 제거하고, 서버관리자가 영속 `hr_role_assignments`의 배정·해제 이력을 검색하고 정확한 `reviewId` 상세를 다시 조회한다. 목록 검색·상태·참여자 성격·업무 범위·서버 페이징, 정확한 상세와 페이지 조정을 각각 독립 UseCase·client·ViewModel·app adapter로 분리했다. 목록과 상세 계약은 내부 사용자 ID와 작업장 IP 범위 원문을 제외하고 사용자 표시 정보, 역할·범위, 근무 시간 조건과 IP 범위 등록 개수만 반환한다. 기능 비활성·로그인 필요·역할 거부·원장 없음·필터 결과 없음·상세 없음·401·403·API 오류·재시도를 구분한다. 이 페이지는 인증이 필요한 `Beta/ReadOnly` capability이며 역할 승인·해제, 근무 시간·IP, 계약·급여를 변경하지 않는다. 이로써 P1의 아홉 워크플로우 대표 조회 기준선을 한 바퀴 닫았다.

2026-07-20 기준으로 참여 인력 관리의 P2 작성 페이지인 통합 웹 `/community/roles/apply`를 완료했다. 로그인 사용자는 서버가 제공하는 제한된 역할 목록에서 하나를 선택하고, 자발적 지원·역할/고용 비보장·최소 검토정보 이용을 각각 확인한 뒤 별도 `hr_role_applications` 원장에 지원을 저장한다. 전화번호·주소·계좌·주민등록번호와 자유 서술형 민감정보는 받지 않으며, 동의 문구 버전과 제출·철회 시각을 기록한다. 제출 요청 ID와 사용자·역할·범위의 활성 키로 중복을 막고, 철회는 본인 지원만 멱등 처리한 뒤 같은 서버 원장을 다시 조회한다. 조회, 제출, 철회와 페이지 조정을 독립 UseCase·client·ViewModel로 나눴다. HumanResourcesManagerApp `/`은 같은 지원 ID를 기존 역할 배정 이력과 구분해 읽되 승인·배정 Command는 제공하지 않는다. 두 페이지는 각각 `Beta/PlatformPersistence`, `Beta/ReadOnly` capability이며 지원만으로 역할 배정·채용·계약·보수·실제 업무를 실행하지 않는다.

2026-07-20 기준으로 알뜰살뜰 마트의 P2 작성 페이지인 주문자 앱 `/food/mart/order`와 통합 웹 `/orderer/mart/order`를 완료했다. 공개 상품 상세의 조회 책임은 그대로 유지하고 주문 작성은 별도 공용 component와 PageViewModel로 분리했다. 로그인 사용자는 상품 한 건과 수량, 비구속 요청 안내 확인만 제출하며 서버는 공개·판매 허용 여부, 현재 투영 수량과 단가를 다시 검증하고 사용자별 `클라이언트요청Id`로 멱등 처리한다. 결과는 기존 출고용 `마트주문`·`마트주문상품`이 아니라 별도 `마트주문요청` 원장에 상품명·단가·수량·합계·재고 기준 시각 스냅샷으로 저장하고, 생성 응답의 같은 요청 ID를 보호 API에서 다시 조회한다. 다른 사용자의 ID와 없는 ID는 같은 404로 처리한다. 수령인·전화번호·주소·결제정보는 계약과 화면에서 받지 않으며, 판매 가능 수량을 차감하거나 재고 예약·결제·주문 확정·피킹·포장·배송을 생성하지 않는다. 음식 주문 화면에 종속돼 있던 인증 표시 ViewModel은 업무 중립적인 주문자 앱 인증 책임으로 분리했고 MAUI와 웹은 각 host adapter만 제공한다. 두 작성 페이지는 `Beta/PlatformPersistence`, 공개 상품 페이지는 `Beta/ReadOnly` capability다. 요청 접수 이후의 취소·수락·재고 예약과 피킹 인계는 P3 상태 전이 전까지 제공하지 않는다.

다음 페이지는 P2의 남은 워크플로우 중 화면 내부 상태나 샘플 저장에 머문 작성 하나를 다시 대조해 같은 세로 기준으로 선택한다.
