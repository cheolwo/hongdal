# WI 문서 → 편집 객체·참여 관계 — D435 첫 실제 저장

## 범위와 판정

[D435 승인](../AI/WI문서-게임객체추출과MySQL연결-2026-08-31.md) SHA `DA31965711AA2A8B35A23D8B0AF02F991AFAC3255C6B8CE926A5B23AACA6E129`와 [D434 구성](게임객체-다중자산-D434-개발통합-2026-08-31.md)을 재사용했다. [D438](../AI/WI전수객체조사-유휴담당분담과DB인계-2026-08-31.md)의 전수 의미 조사는 자료 담당, 공유 계약/DB/검증은 개발 단독 소유다.

**첫3 WI 실제 저장·독립 조회·멱등 검증 완료. 전수 의미 조사/전수 DB 완료 아님.** `WI-NATURE-06`, `WI-FARM-04`, `WI-ACTOR-03`에서 편집 정의9·참여 관계13을 저장했다. 기존 공방을 포함한 DB 합계는 정의10/구성판본10/역할항목5/구성이력10/추출묶음1/참여13/선정자산0이다. 공식 목록105개 중3은 `PartialEvidenceRegistered_NotComplete`,102는 `Unreviewed`다. 실제 Actor/World/Session/재고 생성·게임 실행·E 승격은 없다.

## 정확 원문과 해석

[공식 WI](../../eng/execution-ledgers/world-interactions.json)의 `simulation-world-interactions.r43`/105항목, SHA `B2C54135FB03B4CDCAF5E7191C8CD6F14546782100E635EA75F895F683C53E34`를 직접 읽었다. 첫3의 전체 항목과 관련 기존 지식/감자/Nature 계약을 대조했다. 기존 파생 검색 검사는 `InquirySearch:SpatialProofDrift:docs/AI/다영역-선택형플레이와병행개발-2026-08-31.md`에서 차단됐다. 과거 proof hash를 덮지 않았으며 이번 고정 WI 원문 직접 검증을 검색 전체 신선도 통과로 표현하지 않는다.

| 편집 정의 ID (`design:wi:` 접두사) | 의미/재사용 | 주의 |
| --- | --- | --- |
| player | 세 WI의 공용 행위자 | 실제 사용자/Player 인스턴스 아님 |
| work-npc | Farm의 선택 가능한 위임 행위자 | PlayerOrNpc를 동시 실행으로 해석하지 않음 |
| woodcutting-tool | 장착된 벌목 도구 종류 | 특정 axe.basic·Prefab 선정 아님 |
| tree-resource | 벌목 대상 나무 | 그루터기는 같은 자원의 상태 문맥 |
| timber-log | 벌목 Task 완료 후 별도 결과물 | 작업 시작을 즉시 벌목 완료로 바꾸지 않음 |
| cultivation-unit | 수확 가능한 재배 단위 | 감자 품목·밭 전체·재고와 분리 |
| harvest-lot | 수확 Lot 결과 | 실제 화물 인스턴스 아님 |
| knowledge-source | 승인 지식 출처의 일반 계약 | 물리 형태 `Unresolved`, 모든 출처를 책/NPC로 고정하지 않음 |
| recipe-knowledge | 처방 지식 참조 | `Information`, 실물 책/약초/섭취 결과 아님 |

이 ID는 이번 편집 정의의 식별자이며 기존 Simulation의 구체 품목·Recipe·Instance ID를 대체하지 않는다. 모든 신규 정의는 `d435.r1`/구성판본1/시각 역할0/미선정이다. 13관계 중2개는 능력·노동/체력 조건으로 정의 ID가 없는 `NonObject`다. 저장된 결정 표시는 `CatalogStatement_NotDesignApproval`, 적용 표시는 `NotInstantiated`다. 기존 `design:town:npc-brewing-workshop`의5역할/판본1은 보존했고 WI를 새로 연결하지 않았다.

## 코드·조회 계약

- [WI 계약](../../Ssalddel.Contracts/Admin/Content/게임객체WI참여Contracts.cs), [저장 엔터티](../../Ssalddel.Domain/Content/게임객체WI참여.cs), [가져오기/조회](../../Ssalddel/Services/Content/게임객체WI참여UseCase.cs)를 추가하고 기존 [Context](../../Ssalddel.Infrastructure/Persistence/WorldProjection/개체시각대응DbContext.cs), [관리 Controller](../../Ssalddel/Controllers/Admin/Content/개체시각대응Controller.cs), DI를 확장했다. 같은 Context는 개발만 수정한다.
- 기존 관리 경로 `api/v1/admin/content/entity-visual-bindings` 아래 `POST wi-uses/import`, `GET wi-uses?wi=WI-FARM-04`, `GET wi-uses?definitionId=design:wi:player`, `GET wi-inventory`를 사용한다. 기존 단일 대응/다중 구성 경로는 보존한다.
- 관리자 정책·현재 principal 일치·기능 설정을 검사한다. 임의 경로/URL 읽기 불가, 서버 설정의 고정 WI 원문만 읽는다. 실제 hash·목록판본·WI ID·ruleRevision·허용필드의 전체 문자열 또는 배열 원소를 대조한다. 부분 문자열을 근거 전체처럼 등록하지 않는다.
- 정의 최대32/관계 최대64의 입력 사본을 정렬한다. 정의·구성·이력·참여 묶음을 한 DB 트랜잭션으로 저장하고 실패 시 모두 rollback한다. 멱등 충돌/관계 충돌을 거부한다. commit 직전 원문을 재확인하며 이후 drift는 조회에서 `ReviewRequired_SourceDrift`로 드러낸다. 과거 행 자동 삭제0.
- 현재 입력은 WI JSON 필드만 직접 검증한다. 연결된 Markdown의 독립 절/인용/hash 반입은 미지원이며 D438 결과를 가짜 WI 필드로 바꾸지 않는다. D434 정의의 EvidenceRef/hash는 선언 메타데이터이고 임의 원문 권위 검증 API가 아니다. 향후 등록된 근거 범위의 최소 보완이 남는다.

## 실제 DB 검증

정확 대상은 기존 `hongdal-mysql-1`/`hongdal_dev`, 로컬13306, container `009d54650abf094cd55489f1a456d9ebe23322c00d672ebf74d34c84f17ce78b`다. Compose/실행상태/DB/비root 계정을 확인하고 비밀값은 메모리에서만 사용했다. `local-maintenance:d435` 명시 유지보수 주체이며 기존 사용자 로그인이나 배포 앱 인증으로 위장하지 않았다. 배포 앱의 별도 `ssalddel_dev` 설정/상태는 수정하지 않았다.

새 표 `world_visual_object_wi_batches`, `world_visual_object_wi_uses`와 그 인덱스만 생성했다. 기존 표 ALTER/전체 Migration/EnsureCreated/초기화0. SQL SHA `38F46C0D7037119C5080EE4CEBB80B61B70E261C7A0CACE4DF7A1011D681016D`를 전체 읽고 적용했다.

검증 기록은 `artifacts/local/validation/wi-game-object-extraction-d435/runner/`의 `input.json`, `approved-schema.mysql.sql`, `preview-result.json`, `schema-result.json`, `save-result.json`이다. 결과 SHA `91202F42A712C081D948BBFE3272E9220BD089251DB2C556D92498C9BE467D10`.

- 첫 저장 `Persisted`, 정의9/관계13. 동일 입력 재처리 `Duplicate=true`, 추가정의0/관계0.
- 새 EF 조회의 모든13 입력·UseId·sourcehash/상태 및 신규9정의가 일치. 별도 SQL join의13 UseId 집합도 일치.
- 기존 D431전용3표 전체 행과 D434 공방4표 해당 정의 행들의 hash 전후 동일. 원천9표 count와 공개 품목 identity hash 동일. 비공개 원천 모든 행의 완전 동일성 검사는 아니다.
- CreateNew 기록/claim, 출력 조상 reparse 거부. Runner 최초 빌드는 ListAsync skip 인수 누락 CS7036으로 실패했고 인수1개 보완 후0경고0오류. 실패 당시 DB 실행0. 그 뒤 preview→schema→save 각1회, 저장 내 멱등 재입력1회.

## 시험·남은 일

[집중 시험](../../Ssalddel.Tests/Services/Content/게임객체WI참여Tests.cs)과 기존 개체시각/D434 회귀 합계 **119/119, skip0**. TRX `artifacts/local/validation/wi-game-object-extraction-d435/tests/d435-focused.trx`, SHA `57421954E2230ABC67C23EA00866FF542505C54375D2A5F90148B792BFDA40D9`. 부정 입력·권한·원문 drift·정보형/비객체·중복·변조·SQLite 실패 rollback을 포함하나 실제 HTTP 인증/게임/전수 의미 검증을 대신하지 않는다.

D434의 전체 Task 당시7실패는 [D434 보고](게임객체-다중자산-D434-개발통합-2026-08-31.md)에 분리했다. 이번119통과로 전체 Task를 통과 처리하지 않는다. 후속 범위 Fast `artifacts/local/validation/20260831-203345`는 diff-check·Ssalddel.v3.5.slnx 빌드·집중 시험을 통과했다. D434/D435 두 보고의 로컬 링크21/21 존재를 직접 확인했다.

D435 마감 당시 D438은 HUB8+LOG5 부분조사 반환/추가저장0, D437은 조사입력 단계였다. 이 당시 결과는 보존한다. **후속 실제 등록은 [D438 전용 보고](WI전수객체-D438-개발통합-2026-08-31.md)**에 분리했다: 신규61정의/342관계·최종71/356, 재처리추가0/독립검증·기존보존, 의미7/관계33보류. D437전수메타데이터도별도보고범위이며 어떤정의에도자동할당하지않는다. 이미지/웹과Farm실배치는기존승인후속이며commit/push/Unity/Scene/Save/E/r129/야간재개0.
