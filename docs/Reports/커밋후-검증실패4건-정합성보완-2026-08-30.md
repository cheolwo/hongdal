# 커밋 후 검증 실패 4건 정합성 보완

- 기준: Hongdal `1bc87323`, 시작 시 clean. [커밋 전 검증 기록](공유작업트리-맥락별커밋검증-2026-08-30.md)의 후속 검토다.
- 범위: 기존 승인 기술 통합의 시험·조회 상태 정합성. 게임 규칙·공개 계약·Save·Scene·승인 기획·Goal 활성 목록은 변경하지 않는다.

## 원인과 조치

| 실패 | 확인한 원인 | 보완 |
| --- | --- | --- |
| Unity 계약 어셈블리 금지 | 방문자·방위·LH 표현 모듈이 netstandard2.1 공통 Simulation DTO를 사용한다. 수출항만 Adapter의 JSON 매핑 독립성을 전체 Unity 어셈블리의 참조 금지로 검사하던 기대가 더 넓다. | Adapter 입력·출력 형식 및 중첩 입력 속성의 계약 독립성을 검사한다. 별도로 표현 어셈블리의 운영 서버·Simulation Domain/Application/Persistence/API 실행 참조를 금지한다. 기존 Wire 왕복 시험은 유지한다. |
| Goal 미소유 활성 음성 사례 | `items[8]` Farm이 이미 승인 workItem을 가진 Active 상태여서 시험 변이가 아무것도 바꾸지 않았다. | 실제 미소유 Queued 항목을 선택한다. 전체 Blocked 사례는 모든 작업을 차단하고, v3 읽기 사례는 구형 단일 활성 입력을 구성한다. 관리자 승인·충돌·구형 WIP 검사는 완화하지 않는다. |
| 문답 C# 결속 누락 검사 | Farm01의 공통 실행 메타데이터가 E2로 결속됐는데 계속 미결속을 기대했다. | Farm 결속을 확인하고 모든 미결속 행에 차단 사유 및 실행 불가가 유지되는지 검사한다. C# 결속 자체를 E 승격으로 계산하지 않는다. |
| E8 상태 사본 불일치 | Nature night-day2의 현행 통합 E1에 비해 조회 사본이 E4였고, 기존 PlayableUnit 3개가 전체 E8 대기 목록에서 누락됐다. | `post-e7-evidence-campaigns.r9`에서 관측 사본을 E1로 동기화하고 약초·방문자·병영의 `WaitingForE7` 항목을 보충했다. 모두 promotionEligible=false, 실행 횟수0·증거 없음이다. 실제 Loop E나 E9 구성은 바꾸지 않는다. |

E8 항목의 `evidenceStageCode=E8`은 향후 검증 종류이며 달성 E8이 아니다. 기존 17개에서 20개로 목록을 보충했을 뿐 새 게임 Goal·WI 실행을 시작하지 않았다. 기존 E7 이전의 역사적 실행 증거도 삭제하지 않았다.

## 검증

- 수출항만 Adapter 집중 xUnit: 11/11, skip0. `artifacts/local/validation/post-commit-regression-repair/adapter.trx`.
- Goal 관리: 양성8/음성8 통과, 관리자 Write/Check·결정적 출력 통과.
- 문답 구현 범위 집중 검사 통과: 질문339·대기열62, Bound23/미결속39.
- PostE7 검사: E8 대기20·E9 구성4·보류2·E10 창1 및 음성8 통과.
- Evidence 관리 검사 통과: G1~G5, E1~E3 하위 모듈16, E5 책임 구성 요소7.
- 영향 범위 Fast `artifacts/local/validation/20260830-102937` 통과. Task `artifacts/local/validation/20260830-103038`에서 Unity 솔루션 빌드 경고0/오류0, 전체 xUnit 545/545(skip0), 코드 지도·E 책임 지도 검사 통과. 기존 540개에 실행 어셈블리 경계 검사 5개를 추가했다.
- 이번 Task는 Unity 영향 범위이며 Simulation 전체는 재실행하지 않았다. 커밋 전 Simulation1227/1227과 관리 스크립트 나머지10개의 결과는 과거 증거로 유지한다.
- 승인 인계표의 원문 SHA-256 일치 7/7을 다시 확인했다.

## 남은 작업과 경계

- 지식·방문자 Session/Save v30, 실제 책·방문자 배치, Farm 실측·Scene/Game View 통합은 여전히 미완료다.
- Unity 별도 저장소·미저장 canonical Scene·Editor 상태를 조작하지 않았다. 이 검사는 Unity EditMode/PlayMode/Game View 증거가 아니다.
- 고정된 일곱 승인 원문, workItem 소유·기획 hash·E 상한을 변경하지 않았다. 새로운 기획 결정은 필요하지 않으며 기존 E5 승인 범위로 이어간다.
- 전문 담당에게 쓰기 중지 해제를 전달했다. 신규 전문 작업이나 Editor 점유는 요청하지 않았다. commit/push 없음.
