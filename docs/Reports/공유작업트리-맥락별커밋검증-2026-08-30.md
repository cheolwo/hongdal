# 공유 작업 트리 맥락별 커밋 검증

- 요청: 현재 Hongdal 작업 트리의 변경을 맥락별로 커밋한다. 푸시는 하지 않는다.
- 시작 기준: `9d4fd4c4d1f53d2028970c7a6548ab3e452b6d9b`, `codex/rename-ssalddel`.
- 대상: 기존 변경 357개 파일 및 이 검증 기록. 개발·공간·애니메이션 담당의 쓰기 중지를 확인한 상태에서 검토했다.
- 별도 `C:/Users/user/ssalddel` 저장소와 미저장 Unity Scene은 대상이 아니다.

## 커밋 맥락

1. 수면 안전·난이도·명상·개인 회복 후보 규칙과 시험
2. 개인 계획·열원 상태·세계 자원 재생의 독립 WI
3. Farm 분대 배치·보급·방위 소집·결과·귀환
4. 배치 계획 분리와 LH 표현 수명주기
5. 방문자 임시 체류·지식 기록 표현 준비
6. Farm H2 현행 계약 변환과 동결 시험 자료
7. Farm 실행 문맥·재파종 계보·작업 완료 성장 교정
8. 주제별 문답·기획 이미지·오디오 요구·행동 체력 기획
9. 병렬 WI 작업 관리·등록·E5 명세·원장·생성 상태판
10. 공통 결정·현재 작업·개발 통합 현황 및 이번 검증 기록

공통 원장과 생성 지도는 여러 기능을 함께 반영하므로 별도 통합 맥락으로 묶는다. 각 중간 커밋의 독립 실행을 검증한 것은 아니며 아래 시험은 전체 작업 트리 기준이다.

스테이징 검사에서 새 LH 계약·표현 연결의 `.meta` 두 파일에 있던 빈 필드 뒤 공백만 정리했다(값·GUID 유지). 방문자 애니메이션 인계 보고서의 마지막 빈 줄은 원문 해시를 보존하기 위해 유지했으며 해당 묶음은 EOF 빈 줄 경고만 제외해 검사했다.

WI 등록 생성 문서의 자식이 없는 `wi-family:casualty-response` 행 끝 공백도 생성기 출력 그대로 보존했다. 기본 공백 검사 경고를 통과했다고 간주하지 않으며, 해당 생성 문서만 줄 끝 공백 경고를 제외하고 검사했다.

## 이번 재검증

`artifacts/local/validation/20260830-101818`:

- `git diff --check`, 코드 지도, E 책임 지도 검사 통과.
- Simulation·Unity 솔루션 빌드 통과.
- Simulation 전체 시험 **1227/1227**, 건너뜀 0.
- Unity 전체 시험 **539/540**, 건너뜀 0. 따라서 Task 검증 전체는 실패다.
- 실패: `수출항만인수학당PreviewAdapterTests.CARD_BIZ_1B_UnityRuntime은서버ContractAssembly를직접참조하지않는다`.
- 확인한 충돌: 새 Unity 프로젝트·asmdef·패키지 선언은 `Ssalddel.Simulation.Contracts`를 참조하고, 기존 시험은 같은 어셈블리 참조가 없어야 한다고 요구한다. 계약 참조 구조와 시험 책임의 정합성 검토가 필요하며 이번 커밋 작업에서 어느 쪽도 임의로 수정하지 않았다.

변경된 `eng/tests` 스크립트 13개 재실행 결과 **10개 통과 / 3개 실패**. 로그는 `artifacts/local/validation/context-commits-20260830`에 있다.

| 실패 스크립트 | 첫 실패 |
| --- | --- |
| `codex-playable-loop-goals.ps1` | `CodexPlayableLoopGoalNegativeWasAccepted:active-goal-without-work-item` |
| `evidence-management-systems.ps1` | `PostE7EvidenceCampaignInvalid:StabilityObservedStageStale:stability:nature-night-day2.v1` |
| `playable-loop-inquiry-implementation-scope.ps1` | `GeneratedRegisteredWorldInteractionMissingCSharpBindingGuardInvalid` |

JSON/asmdef 49개 구문 검사와 변경 파일의 충돌 마커·일부 자격 증명 패턴 검사도 통과했다. 이는 완전한 보안 감사는 아니다. 관리 스크립트 시험이 현행 원장을 읽어 문답 구현 상태판·WI 등록 결과·WI/H 공간 상태 JSON 세 생성물을 다시 동기화했으며, 권위 원장과 실제 코드는 바꾸지 않았다. 실패를 숨기거나 시험 기대를 변경하지 않고 개발 담당에게 후속 검토로 인계했다.

## 완료로 보지 않는 범위

- 후보 규칙·독립 서비스·표현 준비 코드를 실제 게임 폐루프 완료로 보지 않는다.
- Save v30은 예약이며 지식·방문자 Session/Save 연결 구현 완료가 아니다.
- Farm 새 밭 연속 두 주기는 기존 체력 비용·회복 경계가 남아 있다. 행동 체력 r12는 기획이며 실제 회복 구현이 아니다.
- 실제 Unity Editor·Play Mode·Game View·청취 검증 및 E5 승격은 이번에 수행하지 않았다.
- 이번 커밋은 현재 작업의 복구 가능한 체크포인트이지 전체 검증 통과나 릴리스 승인 표시는 아니다.
