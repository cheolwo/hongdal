# 전문 산출물 개발 통합 검토

2026-08-30 · D-285 첫 운영 적용 · 검토 전용. 신규 WI·코드·Scene·통합 승인 기록을 생성하지 않았다. 상태 요약은 [개발 통합 상태판](../AI/개발통합상태판.md)이 유지한다.

## 실제 통합 결과

새로 실행 가능한 플레이 기능은 없다. 자원 재생은 공통 프로젝트 안의 독립 Contracts/Domain/Application과 단위시험, WI·Evidence·캠페인 원장 반영까지 확인했다. Farm H2는 외부 작업 트리의 후보 모듈이며 현행 배치 엔진·LH·Unity에 연결되지 않았다.

### 자원 재생

- [최종 인계](../../artifacts/local/remaining-32-wi/resource-regeneration-handoff.json)의 산출물 12개 SHA-256이 현재 파일과 모두 일치했다. Evidence 원장에 같은 ID가 정확히 1개이고 12개 경로·hash 결속도 모두 일치했다.
- WI r43은 구현 `Done/E3`, 통합 `InProgress/E1`이다. Goal r40의 `work:nature-resource-regeneration:logic`은 `Active`이며 `integrationReceiptRef`가 없다. 이번 검토는 이를 `Integrated`로 바꾸지 않는다.
- [서비스](../../Ssalddel.Simulation.Application/RuntimeCore/Simulation세계자원재생Service.cs)는 자체 메모리 원장에 `Create/Get/PreviewTick/ApplyTick`을 제공한다. [LocalSimulationRuntime](../../Ssalddel.Simulation.Application/RuntimeCore/LocalSimulationRuntime.cs)에 이 서비스의 생성·호출 연결은 없으며 조사한 Application/Domain/서버/Unity C# 참조에서도 실행 소비자는 집중시험뿐이었다.
- 그러므로 독립 서비스 안의 Tick 성공과 실제 게임 Session Tick에서 자원이 재생되는 것은 다르다. 기존 게임 Save·RemoteHost·Unity 실행은 인계에서 제외한 범위이며 이 누락을 E3 실패로 바꾸거나 구현 완료로 숨기지 않는다.
- 이번 공유 프로젝트 집중 재실행은 **35/35 통과**했다. [새 TRX](../../artifacts/local/integration-review-20260830/resource-review.trx)에 보존해 기존 인계 시험 파일·hash를 덮어쓰지 않았다. 과거 77/77 회귀는 이번 재실행이 아니다.

## 준비됐지만 미통합: Farm H2

원문은 `C:/Users/user/.codex/worktrees/cba3/Hongdal/spatial-support/farm-h2/HANDOFF.md`와 `delivery-manifest.json`이다. 원문 산출물 16개 hash, 자산 6종의 Prefab/meta 12개 hash가 일치했다. 기준선 20개 중 16개가 현재와 일치하고 AGENTS·DECISIONS·CURRENT_WORK·생성 Goal 상태판 4개는 달라졌다. 현재 코드·문답 기준선이 모두 낡았다는 뜻은 아니다. 구체 비교는 [기준선 검사 JSON](../../artifacts/local/integration-review-20260830/farm-baseline-audit.json)에 기록했다. 원본 기준선이나 인계 hash는 갱신하지 않았다.

| 경계 | 생산 후보와 현행 소비자의 차이 | 다음 승인 작업에서 필요한 것 |
| --- | --- | --- |
| 프레임워크 | 후보 프로젝트는 `net10.0`; 현행 Contracts/Domain/Application은 `netstandard2.1`, C#9 | 독립 사전 생성 도구 유지 또는 호환 소스 이식 중 실행 위치를 정한다. 그대로 ProjectReference하거나 Unity에 DLL을 넣을 수 있다고 판단하지 않는다. |
| 소스/API | 후보는 file-scoped namespace·새 컬렉션 구문·`SHA256.HashData`·`Convert.ToHexStringLower` 등 사용 | 직접 이식은 문법·API·참조 라이브러리 검토가 필요하다. 이번에는 이식·소비 빌드를 하지 않았다. |
| 형식·공간 | 후보는 H1·AABB·Bottom·Yaw·Anchor/Route·VisualKey/AssetFamilyId. 현행 계획은 CellStableId·OwnerCellStableId·LocalXYZ·UniformScale·CompositionKey·부모 객체·지도 hash 필요 | 승인된 세계 문맥의 좌표 원점·셀/H 소유와 실제 자산 pivot/크기·해결 키를 명시적으로 매핑한다. AABB 중심이나 시험 치수만으로 실제 Prefab 위치를 확정하지 않는다. |
| 해시 | 후보 ResultHash는 JSON 직렬화 SHA-256. 현행 배치/분리/LH는 각자 정규형 hash와 SourceWorldRevision을 보존 | 후보 hash를 현행 `AssetPlacementPlanHashSha256`에 복사하지 않는다. 변환 판본·입력/출력 hash 계보를 남기고 현행 형식으로 봉인한다. |
| 분리 계획 | `Partition` 입력은 이미 봉인된 `Simulation세계자산배치Plan`이다. 후보 형식은 이 타입이 아니다 | 순수 변환 Adapter가 먼저 필요하다. 기존 Compose로 새로 배치해 동결된 후보를 다른 결과로 바꾸지 않는다. |
| LH | `Prepare/Transition`은 기존 분리 계획·셀·revision·hash를 소비한다 | 위치·Seed·자산을 재선택하지 않는 기존 수명주기 경계를 재사용한다. 이번에는 후보→분리→LH 통합시험을 하지 않았다. |
| 재사용 계산기 | 후보의 ReusedSurfacePlacement는 Unity 표면/배치 소스 동결 발췌다. 기준선의 해당 Unity 소스 hash는 현재 일치한다 | 장기 복제본으로 원본과 별도 발전시키지 않는다. 다음 작업에서 Adapter 또는 호환 공유 계산 경계로 중복 소유를 정리한다. |
| 실제 공간 | 9점·통로 표본과 시험 AABB 검증. 실제 Prefab 치수·문·실내·충돌·NavMesh는 없음 | 실제 자산 측정·상호작용점·지지면·통로를 확인하고 별도 후보 승인 후 표현 작업으로 진행한다. |

근거 소비자: [계약](../../Ssalddel.Simulation.Contracts/UnityPackage/Runtime/SimulationWorldAssetPlacementContracts.cs), [분리 서비스](../../Ssalddel.Simulation.Application/SimulationWorldAssetPlacementPlanPartitioning.cs), [LH 수명주기](../../Ssalddel.Simulation.Application/SimulationLhAssetPlanLifecycleService.cs). `Partition`의 입력 형식 검사를 전체 후보 기하/내용 검증으로 오인하지 않는다.

담당이 보고한 Debug/Release 각 50/50은 보존된 **독립 검사 실행기**의 결과다. 이번 개발 검토는 소스·형식·기준선 대조이며 이 50개 시험이나 실제 Unity 실행을 재수행하지 않았다. 산출물 수신·해시 일치·호환성 검토는 패턴 승인이나 H2/E 승격이 아니다.

## 애니메이션: Nature 벌목 한 건 재조사 수신·대조

[2026-08-09 설계](../Architecture/UnitySyntyAnimationReuseAndRetargetDesign.md)의 `.anim/Controller 없음`은 당시 기록이다. 현재 [사용 프로필](../../eng/execution-ledgers/synty-asset-functional-modules.json)에는 locomotion/emote/sword-combat 후보가 있으나 사용 프로필만으로 실제 Clip·Avatar·Controller 호환을 증명하지 못한다.

애니메이션 스레드 `01a04676-8d10-7480-b851-707fbd655d46`의 2026-08-30 읽기 전용 인계를 개발이 수신했다. **Nature 벌목/도구 사용 한 가지**만 대상으로 했으며 공용 여섯 의미 키는 연결 비교에 사용했다. 개발은 후보 FBX/meta12개·Controller2개·도끼 Prefab/meta2개·Farmer Prefab1개·표현 소스4개, 총 **21개 hash를 직접 대조해 모두 일치**시켰다. [검사 JSON](../../artifacts/local/integration-review-20260830/animation-hash-audit.json)에 경로와 원문 기대 hash를 보존했다.

| 구분 | 확인된 사실 | 여전히 증명하지 못하는 것 |
| --- | --- | --- |
| 원천 후보 | Idle/Walk/Run·Sword Guard/Attack/Stagger FBX와 meta가 존재. Base Controller의 Idle/Walk/Run GUID·fileID는 meta 선언과 일치 | Unity가 실제 가져온 Clip 객체·곡선·대상 Avatar 유효성·재생 성공 |
| 저장된 Rig 오류 | Walk와 Run meta의 `rigImportErrors`에 `pelvis`/`Hips` 불일치가 남아 있음 | 현재 Editor에서 같은 오류가 재현되는지 여부 |
| 현행 Adapter | fallback 분기는 Controller를 비우고 뼈 확보 후 Animator를 끈다. 비fallback Configure 분기는 진단 문자열만 정리하며 Clip/Controller를 지정하지 않음 | Clip 보유만으로 게임에서 재생됨 |
| 현행 벌목 | Nature Presenter는 작업 상태에서 카메라/RightHand 도구와 양팔·척추를 절차적으로 움직인다. Generic 도끼 실물과 소스가 존재 | 손 접촉·타격 프레임·양손 IK·중단 복귀의 화면 품질 |
| 작성자 충돌 위험 | 공용 Adapter와 Nature Presenter가 각각 LateUpdate에서 팔·척추를 쓴다. 두 소스/메타에서 명시적 실행 순서 설정은 찾지 못함 | 실제 Game View 충돌 재현. 프로젝트 전역 실행 순서까지 검사한 것은 아님 |

담당의 추가 팩 정적 집계는 Base/Emote/Sword 각각 FBX721/283/242, Controller174/0/1, `.anim`0이다. 명시적 meta Clip 이름 행 808/284/365도 보고했으나 이는 **가져온 Clip 총수나 재생 가능한 Clip 수가 아니며 개발이 전체 집계를 재실행한 결과도 아니다**. 과거 823/280/363 집계와 단위가 확인되지 않아 숫자를 대장에 덮어쓰지 않았다.

전용 벌목 Clip은 담당의 이름·metadata 조사에서 찾지 못했다. 검 공격 후보를 도끼 벌목 동작으로 채택하지 않는다. 현재 유지 가능한 것은 기존 절차형 벌목과 도끼의 **코드 연결**이며, 이번에 새로 재생 가능하다고 인증한 Clip은 0개다. reimport·Rig 수정·Controller/Adapter 구현·Editor·Play Mode·시험은 이 조사에서 수행하지 않았다.

## 필요한 기획 결정과 다음 작업

1. 흔적 조사: [승인·등록 인계](Nature자연흔적조사-승인등록인계-2026-08-30.md)의 서로 다른 근거/종류 조건과 미정 수치 경계 검토 후 승인 revision·hash 결속. 현재 신규 코드 착수 불가, 가장 이른 재개 Logic E1.
2. 자원 재생: 현재 E3 인계 수락과 Session 실행 연결을 분리한다. 정식 E3 `Integrated` 처리가 필요하면 해당 작업 명세·현재 파일 hash·검증을 묶은 Accepted 통합 기록으로 남긴다. 이 기록으로 Session·Save·UI 범위를 열지 않는다. 이번 검토에서는 기록 생성·상태 변경을 하지 않았다.
3. Farm: 후보→현행 배치 계약의 비실행 변환·정합성 시험을 다음 좁은 승인 작업으로 제안한다. 독립 사전 생성 도구 유지와 공통 코드 이식은 기술 실행 방식이며 치수·H 배치·자산 승인과 구별한다. 이번 후보를 Scene에 반입하거나 개발이 임의로 승인하지 않는다.
4. 애니메이션: 대상 Actor/Rig 한 개와 AnimationRole/ActionCue·1/3인칭·도구 접촉·중단/복귀 수용 기준을 후속 승인 연구에 결속한다. Editor 점유·읽기 검증 범위 승인 후 imported Clip/Avatar와 저장된 Rig 오류를 확인한다. importer 수정·리타기팅·새 Adapter는 별도 승인 작업이다. LateUpdate 소유권 위험은 개발 검증 과제이지 사용자가 임의로 실행 순서 숫자를 정할 문제는 아니다.

두 기존 전체 회귀 실패(공간 모판 고정 revision·하위 모듈 고정 개수)는 별도 기술 검증 과제다. 이를 기획이 숫자만 선택해서 해결할 문제로 넘기지 않는다. 이번 범위는 문서·검토·자원 집중 재시험이며 전체 회귀 통과·Runtime·Game View·새 WI 구현·commit·push를 주장하지 않는다.
