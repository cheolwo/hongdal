# LS01 데이터 후보 인수와 배치 소비 경계

기준: 2026-08-30 / 개발 정적 검토. [전문 최종 인계](Farm경관-LS01-전문인계-2026-08-30.md)와 [D363 배치 고도화 우선순위](../Architecture/지도구성과세계자산배치분리.md)를 따른다. 이 보고서는 후보 승인·새 실행 명세·실제 배치 증거가 아니다.

## 인수한 자료

### D376 회랑 표시 수정의 실제 Play 대조 — 21:49 반환

Farm 명세 `A2DA9E76F860EBC8CB19CEE36B8F5908E040C122F94239759EAEAD40246607A0`/Goal r68의 좁은 회랑 표시 수정이다. 기존 `H계층배치증거Root.ApplyHierarchyOverlay`가 전용 회랑 Renderer에도 기존 표시 flag를 적용하도록 변경했고 격리6시험이 retry3 정식169/169에 포함되어 통과했다. H3 지지면·Collider·H 계보·Scene 파일은 변경하지 않았다.

공간 run-2145 결과에서 실제 CorridorRoot 참조·3자식/Renderer3·Collider0 및 Play Awake의 flag=false/Renderer3=false를 확인했다. 개발은 아래 실제 UI 포함 출력과 정지 Editor 창 이미지를 직접 열어 구분했다. 공통 경로는 `C:/Users/user/ssalddel/artifacts/local/validation/farm-landscape-ls01/overnight-d375/world-continuity-d376/run-2145-corridor-fix/`다.

| 파일 | 개발 확인 / SHA256 |
| --- | --- |
| game-output-diagnostic-visible.png | 같은 실행에서 정확 회랑 Renderer만 켠 진단 대조, 과거 버전 실행이 아님 / `4797A8DD76F940AC6DD6DBF49434B3F44E53B936F343B4B9B6309F6ABA87EEE5` |
| game-output-restored-hidden.png | 기존 표시정책으로 회랑 선 숨김. 상좌측 H3/LH 색면 경계는 남음 / `B345DADA12AB274C115D660CFE69D905EA66FAEC552989BA41E967550774FCEE` |
| game-output-reentry-settled.png | 정상 Stop 뒤 두 번째 Play에서도 회랑 기본숨김 / `20B981C1E6ECF76F08D2B2FFD0FA450E0E95BE7E9474236930A26EEB6AED0ED8` |
| stopped-editor-white-object.png | 정지 Editor Game 화면의 구름 가림. Play 화면이 아님 / `75274F8590611C09C128F1D7F9E93846C7179D502F4017979B4D54039100C7D2` |

실제 우클릭 횡단 후 출발점 약0.093m 이내 복귀·hasDestination=false/blocked=false를 관찰했다. 동기 표시 토글 안에서는 타 Renderer·Collider/TRS·Session 불변이지만 PNG 사이 실시간/안개 변화가 있어 완전히 동결한 시각 품질 비교는 아니다. 두 차례 Play/Stop 후 4701객체 직렬 상태·저장Scene/Packages2/Save2 hash와 수정시각·소비Assembly4 불변 및 Console7557→7593 신규error0은 전문 종료 보고다. Scope null→복원Session53은 별도 변화이며 전체 메모리 불변으로 확대하지 않는다. Sky 모델 직렬화의 Color 순환참조 도구오류1건은 Console 오류와 구분한다.

D378 중앙 흰 객체는 실제 카메라 ray/mesh 교차로 `SimulationWorldShell/SkyEngineVisualRoot/Clouds/SyntyCloud_03`, 좌상단은 `SyntyCloud_01`로 식별됐다. 원본 삭제나 색상 일괄삭제는 없었다. 생성기의 기본 구름 활성과 권위 기상 공급 뒤 표시를 분리 검토하며 정확한 수정 경로 결속 전에는 변경하지 않는다. 회랑 한 결함의 실제 개선이며 전체 배경 연속성·흰 객체 정리·Farm B 조립·벌목/E5 완료가 아니다.

### 고정 변환 코드 최종 인수와 개발 재검증

- 전문 최종 보고 SHA256 `BC34F1502A2E677DEA82D5D242710DF6A6C3273B57C118A8B4D73E7AD33FB44E`, `artifacts/local/validation/farm-landscape-fixed-conversion/handoff-manifest.r1.json` SHA256 `C268AEC05DD9F6AF800772141AEAD6EF289EB08DAF0EE4DB995F55932F8756DC`. 등록 코드6/Fixture4의 현재 hash10/10 일치, 최종 TRX155/155·실패0·skip0을 개발이 직접 확인했다.
- 공통 검사 추출·고정 변환·표본 일관성/원본 계보·출력 변조 거부·Partition/LH 시험을 검토했다. B 정규형은 XYZ 배열 순서를 보존하며 원A의 정규형을 변경하지 않는다. Unity 단일 Prefab 키는 `candidate:ls01:single-prefab:<GUID>` 후보로서 실제 Resolver 등록 완료가 아니다.
- 개발 Fast `artifacts/local/validation/20260830-195411`: 공통 지도 검사/3프로젝트 빌드/집중155 재통과. Task `artifacts/local/validation/20260830-195446`: Simulation 솔루션 빌드/전체1544/1544·skip0 통과. `dotnet test Ssalddel.Unity.slnx --configuration Debug --logger "trx;LogFileName=unity-consumer.trx" --results-directory artifacts/local/validation/farm-landscape-fixed-conversion/development-consumer`로 Unity 공통 소비자545/545·skip0도 통과했다. 이는 Unity Editor 컴파일/Play 증거가 아니다.
- 공통 지도 생성 결과 E 책임 후보765/표기762/제외3/미분류0. CodeMap의 기준 기능 밖 탐색 후보 경고는 남으며 오류로 은폐하거나 신규 기능 결손으로 단정하지 않았다. Farm 명세 `D8DDCE4EA84F0CE7846207DDE50B81EAE4DB5BA980BF92D1022BDB4A7502A42D`/Goal r63 재결속 및 검사 통과.
- 원A5+B8의 결합 Plan `0ce526926ce264ee4ac4d1960a1787b0135b6ae85cba12c613c919a27d1986ba`, Result `3fac653425d94919b9c0b1e011266144a1a88c9e110a04cf8089e279309a4418`, 지면은 `SyntheticFixture` 1170좌표다. 원본 A/B/실측/연구 불변과 제공 표본에 대한 성공이며 실제 지형·Player 통행·조립·GameView 또는 E5 완료가 아니다. 후보는 UnapprovedCandidate를 유지한다.
- 다음 기술 연결: 실제 단일 Prefab Resolver/동결 지면 사본/셀 좌표·판본 공급→기존 조립 소비에서 기대13개와 보정량·누락/경고를 확인한다. 그 뒤 공간 담당의 동일 지형·카메라·시간 A/B 캡처로 이어간다. 신규 Scene/자동축소/누락 지면 fallback/후보 시각 승인/E 승격은 이번 인수에 포함하지 않는다. D359 Task/AudioFX 연결은 독립 진행 범위로 유지한다.

아래 자료는 각 이전 배분·검증 시점의 이력이며 위 코드 인수보다 앞선 데이터 전용 상태다.

커밋 후 다음 구현 배분: 전문 제안 SHA256 `7BDB4241C015D4ADABF818ABD49C26945F971A3F20BD19EC4E6626A2050807F2`의 후속절을 검토했다. Farm 명세 `B722A3ECD02A43EBF57FC9E371E96148D25E91D9ACC42E0FD23BBA12E8843F4F`/Goal r62 검사 후 기존 공간 작업에 고정 변환 Contracts/Service·공통 적합성 helper·호환/변환 시험·Fixture 경로를 결속했다. 기존 `SimulationFarmH2PlacementAdapter.cs`의 단독 쓰기도 같은 공간 담당에게 이전해 추출/호출 치환을 한 묶음으로 수행한다. 개발은 Tests csproj에 새 Fixture 복사 설정만 추가했다. 원A의 규칙/역할/H1/정규형과90회귀를 보존하고 B8의 고정 TRS를 별도 검사·봉인하며 LH 자체를 재설계하지 않는다. 이 배분은 구현 착수이며 새 변환 시험이나 실제 화면 성공 증거가 아니다. Unity 연결 패키지의 새 Contracts 작성 시점은 D359 Editor 시험 종료와 조율한다.

- 전문 보고서 SHA256: `4FB3D6E9217B8E72E1C17E787F4429AC73C77DB964A545F2DEF1ED798B85926B`.
- Unity `artifacts/local/validation/farm-landscape-ls01/comparison-preparation/handoff-manifest.json`: `B3AE1C730AD3C9D115ED6D409DA22092D313CA7F05402B5719F3E4DBD0E22CE2`. 개발이 등록11파일의 현재 hash 일치를 확인했다.
- `frozen-r1/validation.json`, `repeat-r1/validation.json`을 직접 읽어 각각34개 `pass=true`/실패0을 확인했다. 개발이 시험이나 격리 빌드를 다시 실행한 것은 아니다.
- 두 실행의 `B.delta.json` 파일 SHA256은 모두 `3E2D3C7B395CB4B1622D5B7B38971CB84A66B69B1D3CA50ACC4F8062C79C97DE`다. 정식 소비 검토 기준은 `frozen-r1/`이며 `run-01/`은 중간 이력이다.
- A는 기존 확대 Barn01 후보, B는 `farm-landscape-ls01.flat-ab.r1`의 6종8장식/nativeScale1 변화분이다. B input/output hash는 각각 `e8c00fb8e51b70ad9a178889c7b09733bbb42483bb8c29663c77424b10afa0de` / `63a08b079c947b9370a32f8d45af0044e4fab16abbd80df683aa723ffe2eb704`다.

B는 `UnapprovedCandidate`, Core 소비 불가다. 기존 검사에 B 외곽을 임시 장애물로 넣은 통로·객체 진단은 B의 H/구성 키/지지면을 검증한 것이 아니다. Partition/LH 시험은 A에만 적용됐다. 도로·마당 표면2개는 기존 `RouteObstacle` 거부로 보류하며 검사 완화·자동 축소·Collider 제거로 통과시키지 않는다.

## 현재 C#와의 호환성

첫 추출 결과: 전문의 `artifacts/local/validation/farm-landscape-fixed-conversion/extraction/after-extraction-confirmed.trx`를 개발이 직접 읽어 기존90+호환10=100/100·실패0·skip0을 확인했다. TRX SHA256 `0D46489533D68215B3F1AB87ACBD866D8A58F2C30A6EA2A565FBF853087D8ABB`. Adapter→공통 helper 호출 치환과 지지/간격/보호/통로 검사 코드를 대조했으며 B 변환은 아직 작성 중이다. 이 결과는 원A 호환 추출 단위이며 최종 B 코드 인수/실제지면/Scene 성공이 아니다. 최초 골든문자열 전사 오류 수정은 전문 초기로그로 보존한다.

| 경계 | 실제 코드 근거 | 다음 구현 시 지킬 조건 |
| --- | --- | --- |
| Farm 후보 변환 | [SimulationFarmH2PlacementAdapter](../../Ssalddel.Simulation.Application/SimulationFarmH2PlacementAdapter.cs)의 `ValidateBinding`은 H1/Anchor, 고정 역할·VisualKey와 정확한 후보 판본을 요구한다. | B를 grass 역할이나 r2 Farm 후보로 위장하지 않는다. 이 전용 검사를 느슨하게 만들지 않는다. |
| 공통 환경 배치 | [공통 배치 계획](../../Ssalddel.Simulation.Application/SimulationWorldAssetPlacementPlanning.cs)의 환경 발생 경로는 `Environment`·OwnerCell·CompositionKey를 사용하며 H1을 설정하지 않는 경로도 있다. | 환경 장식마다 신규 H1/게임 WI가 필요한 것은 아니다. 실제 Cell/상위 H 계보와 영역 내 소유를 별도로 검증한다. 기존 확률·좌표·scale 계산을 B 동결 좌표 대신 재실행하지 않는다. |
| 계획 분리 | [계획 분리 서비스](../../Ssalddel.Simulation.Application/SimulationWorldAssetPlacementPlanPartitioning.cs)는 실외/실내 파생과 hash를 구성하지만 B의 실물·지지면·구성 키 승인 검증기가 아니다. | 완전한 입력 검증과 정규형 계획 봉인 뒤 기존 Partition/LH를 소비한다. 분리 성공만으로 승인 처리하지 않는다. |
| Unity 자산 해결 | Unity `Assets/Ssalddel/Presentation/World/World공간표현조립Coordinator.cs`의 `실외자산배치Runtime.ResolvePrefab`은 일부 Nature 키 또는 Composition Catalog를 소비한다. 기존 수목완충지 구성은 여러 객체 묶음이다. | B의 단일 Prefab 8배치와 다중 객체 구성을 구분한다. 장식 나무에 채집 자원 상태나 Nature 행위 표식을 임의 부여하지 않는다. |
| Unity 결과 판독 | 같은 Runtime은 선택형 `AmbientPresentation` 자산 누락/정렬 실패를 건너뛰고도 전체 성공을 반환할 수 있다. `ObjectCount`/`TryGetObject`가 존재한다. | 셀 Active/Apply=true와 B8개 발현 성공을 분리한다. 기대 ID별 생성·Renderer·필요 Collider·경고를 확인한다. 기존 선택형 실패 정책을 통행 차단으로 바꾸지 않는다. |
| 표면·pivot | 같은 Runtime의 `AlignAndValidate`는 보이는 하단을 표면에 다시 정렬한다. B의 Y는 합성 지면과 Renderer 여유 계산이다. | 동결 pivot/좌표와 런타임 정렬 보정량을 구분해 기록한다. 실제 지지면·Collider 접촉·경사·부유 검증 없이 실제 접지 성공으로 보고하지 않는다. |

공통 DTO와 환경 배치 경로가 이미 있으므로 새 LH 시스템이나 새 저장 판본이 필수라고 판단할 근거는 없다. 반대로 DTO에 필드를 채워 직렬화할 수 있다는 사실도 B 소비 검증 완료가 아니다.

## 담당별 다음 작업

1. 공간 담당: 기존 전문 보고서의 새 후속절에 B6종8개 VisualKey/단일 Prefab/Composition 후보, Cell·상위 H 계보, Renderer·Collider·지지면 입력 대응표를 작성한다. 기존 동결 자료·Accepted 연구는 변경하지 않는다. Editor/Scene/공통 코드 쓰기는 배분하지 않았다.
2. 개발 담당: 대응표를 받아 기존 공통 배치 검증 재사용 범위와 LS01 전용 입력 검증의 책임을 정하고 명세·쓰기 경로를 결속한다. 미등록 키·소유 누락·잘못된 측정hash·지지 없음·통로/보호영역 침범 거부 및 A 불변 회귀가 첫 코드 수용 기준이다. 이는 승인된 경관을 연결하는 기술 작업이며 미해결 기술 선택을 사용자 미승인으로 돌리지 않는다.
3. 검증된 배치 계획 이후에만 LH 준비/활성/캐시/해제/재진입을 좁게 확인하고, Unity 조립과 공간 담당의 동일 카메라/시간대 A/B Game View 검증을 연결한다. B 현재 카메라·Sky·Session 동결과 실제 화면은 미검증이다.

현재 새 플레이 의미 결정 요청은 없다. Q392 표면의 통로 의미 변경이나 생산·충돌 규칙 변경이 필요해지는 경우에만 그 영향 범위를 따로 반환한다. Q393~396 수관/조명/기상/실제 생활 상태 공급은 해결됐다고 가정하지 않는다.

## D363 진행 요청 이후 첫 코드 결속

D364의 통합 실행/해제 방향도 읽었다. 이번 사전검사 구현을 멈추지 않고 다음 연결의 기준으로 사용한다. 정적 대조상 Unity `공간LHStreamingEngine.RequestWindowAsync`에는 요청 세대·취소 후 낡은 응답 거부가 이미 있고, `NotifyAuthorityRevision`·`ApplyPreparedCell`·`CacheCell`·`ActivateCell`·`ReleaseCell` 호출 경로도 있다. 새 대형 조율자를 추가해야 한다고 판단하지 않는다. `SimulationLhAssetPlanPresentationReconciler`는 별도의 순수 Prepare/Activate/Cache/Release 명령 계산이며, 이번에 검색한 Unity Assets/Ssalddel 및 Ssalddel.Unity 범위에서는 Streaming의 직접 소비를 찾지 못했다. 두 경로를 동시에 연결하기 전에 기존 요청/관심 소유·해제 기준을 대조해야 한다. 공유 관심의 최종 해제, 최신 사본 재진입·성공 명령의 표현 실패 비롤백은 후속 시험이며 정적 존재만으로 통과 처리하지 않는다. 셀 표현 해제는 작물/NPC 상태 삭제나 Session 종료가 아니다.

사용자의 후속 진행 요청에 따라 Farm 명세 `1D8E74DBC4F53CB6245AB2508BAF5251B9C2C29C21C84B1730E6D223B994ABD9`와 Goal r60의 `work:farm-crop-cycle:landscape-binding-guard`를 결속했다. Goal Write·작업 명세·주제 Validate 및 전달 Check·문답 파생 Write가 통과했다. 승인 원문/연구 hash와 기존 E는 유지한다.

- 개발 소유 신규 경로: `Ssalddel.Simulation.Application/Simulation경관배치소비검증Service.cs`, `Ssalddel.Simulation.Tests/Simulation경관배치소비검증Tests.cs`.
- 첫 코드 책임: 기존 공통 계획과 기준 A, 기대 추가장식 목록, 지도/H 계보, 실제 해결기의 등록 키를 입력받아 정규형 hash·원A 불변·기대 집합·소유·Scale1/유한 좌표·비권위를 검증한다. 결과는 `BindingVerified`와 실제 조립에서 확인할 기대ID 사본이다.
- B JSON을 읽어 공통 계획을 만드는 변환기나 실제 Unity 소비자는 아직 아니다. 합성 결속 Fixture 시험은 실제 B 지면·통행·발현 시험과 구분한다. 새로운 Save/LH/게임 WI는 만들지 않는다.

공간 대응표 후속 보고서 SHA `1A15EF8940E2A8CE2D2F53BAB0C10E96BCB3C444F96F65977AF2CBBD5062EBFC`를 직접 읽고 hash를 확인했다. 이전 manifest11/11 일치는 수신 당시 기록이며, 이 보고서가 후속 변경된 현재에도 이전 manifest 전체가 일치한다고 주장하지 않는다. frozen B와 연구는 별개로 보존한다. 표의 단일6자산 키는 미등록 후보이며 기존 다중 수목 구성과 교체하지 않는다. `CollisionEligible`는 현재 Unity Configure에서 Collider 제어에 사용되지 않으므로 그 필드만으로 실제 물리 조건을 증명하지 않는다.

이번 코드의 다음 연결은 실제 등록 키 해결→동결 B 변환→기존 외곽/지지/통로 검증→조립 결과의 기대ID 대조다. LH는 검증된 계획의 수명주기만 소비한다. 승인 경관을 다시 문답 대기로 돌리지 않는다. 과거 Graph2 잔존과 Console 기록은 이번에 재조회하거나 해소한 것이 아니다. Scene/Save/E 변경·commit/push 없음.

## D375 실제 지면 조사 좌표 준비

최신 반환: 공간 slot2의 실제1170표본(SHA BDB63DE5D75F410B378B52EE75F936BC1B72F5F7A8DA78A533DBB38F4761F2F6)과 manifest16/16을 개발이 대조했다. Provider0m/Collider약-0.05m 및 실제H4/H2 계보 미결속을 분리하며 전체 지지 승인/고정 변환/조립은 아직 없다. UI 포함 실제 출력 `C:/Users/user/ssalddel/artifacts/local/validation/farm-landscape-ls01/overnight-d375/slot2/game-output-ui.png`(SHA1E1AB592D50371ECC57F45C1B552A39D079948CDA999C31148C19C92D073C380)를 개발이 직접 열어 기존 Nature와 하단 모드 버튼을 확인했다. 신규Farm B 성공이 아니다. 디스크5파일/4701객체 직렬값 보존과 Graph/Scope 변화는 별도다.

D376 추가 승인 원문 SHA C5536D2A9D707C84FDD1AE13FDCCCCF2C7A175FAC094FE1CA3049F80899E2812 전체 대조 후 Farm 명세7D3D9E92D0D2E490F305262847B5533DD5BEC764845CF2637A7E6A949E54C16C·Goal r66을 검사하고 공간 담당의 다음 단독 슬롯에 배경 경계 진단을 배분했다. 쓰기는 기존 전문보고서 후속절과 overnight-d375/world-continuity-d376 artifacts뿐이다. 셀 수명/카메라·LOD/지면·식생별 원인을 먼저 대조하고 같은 조건 UI 전후·왕복·성능 관찰을 반환한다. 공통 수정은 정확 경로 추가 결속 뒤, 새 원경 구성은 Required 연구 후속 Accepted 뒤 진행한다. 기존 연구/후보/D375/09:00/E상한은 유지한다.

공간 첫 Play 사본의 실제 Farm 셀은 `kr5186:l3:2801:4581`, 원점 `(15.4,0,10.25)`다. Profile AreaSet/H4는 `farm-hub-town.v1`이고 현재 고정변환은 `farm-production.v1` H4를 요구한다. Handover의 farm-production 기본 문자열은 topology unavailable/Candidates0이므로 실제 공급 근거로 대체하지 않는다. 현재 연결 공백이며 ID를 치환하지 않았다.

개발은 artifact 전용 `farm-landscape-fixed-conversion/overnight-d375/coordinate-request/CoordinateRequest.csproj`와 `Program.cs`를 작성해 현행 시험 DLL의 Input/Convert를 읽기 재사용했다. 격리 도구 실행 종료0, 동일 합성 입력2회의 Plan hash `0ce526926ce264ee4ac4d1960a1787b0135b6ae85cba12c613c919a27d1986ba` 불변과 1170개 요구 좌표를 확인했다. 기존 fixture 높이는 출력하지 않고 local 좌표만 추출하여 실제 사본 원점에 옮긴 조사 world 좌표를 함께 기록했다. 공유 프로젝트/Unity/패키지 빌드·변경은 없다.

산출물 `artifacts/local/validation/farm-landscape-fixed-conversion/overnight-d375/coordinate-request/actual-coordinate-request.r1.json`, SHA256 `B6E130673105D888BFA91B7C6A82CED9B513F09450C226EB06903007F2E2EC9C`. 입력 실제7점 파일과 소비 DLL3개 hash도 포함한다. 상태는 `ReadOnlyInvestigationRequestNotValidatedPlacement`; 소유권 결속·실제지지 통과·조립허용은 모두 false다. 실제7점은 Provider0m/Collider약-0.05m로 다르며 전점 측정 대신 사용하지 않는다. 다음 공간 슬롯에서 원점/판본 재확인 후 Provider와 Collider 값을 각각 수집해야 한다. 현재 후보 시각승인·실제 통행·Farm B GameView·E 상승 증거는 아니다.

## D423 선언검사 분리와 D422 관찰코드 인수

아래 D422 준비 착수의 후속 결과다. [D423 승인](../AI/배치책임구분-리팩토링승인-2026-08-31.md)과 두 책임 기준을 Farm 명세 `d423Refactor`에 결속한 뒤 Unity의 정확2파일만 변경했다.

- `Assets/Ssalddel/Presentation/World/배치통제계층Root.cs`: 상향 성립 선언/감각 채널의 기존 조건식을 private2메서드로 추출했다. 실행식·단락 평가/실패 순서·예외문구·공개 API/Configure/직렬화 필드와 meta는 보존했다. 구조 선언의 성공은 실제 지지·통행/배치 성공이 아님을 주석으로 구분했다.
- `Assets/Ssalddel/Tests/EditMode/Hub창고정밀배치Tests.cs`: v1~v4/손상/거부순서/입력불변13사례를 추가했다. 소유 Preview1과 비활성 자손만 만들고 finally 자기 객체/Preview만 정리한다. 기존 Scene 열기/Player 검색/기존 Controller 초기화는 없다. 기존 시험은 바꾸지 않았다.
- `artifacts/local/validation/farm-world-map-d422/preparation-01/d423/source-verification-r3.json`: 원식 역치환·meta·기존시험·13사례·비활성/소유Preview 경계 정적10항목 통과. 앞 r2의 실패는 검증식이 실제 변수 `ownedRoot/ownedScene` 대신 `root/previewScene`을 찾은 오류이며 원결과 보존 후 검사식만 정정했다. 이것은 Unity13시험 실행이 아니다.
- Unity 기존 생성 EditMode 프로젝트의 외부 컴파일 성공: 첫 `--no-restore`는 Temp assets 부재 NETSDK1004, 정상 restore 포함 전체 컴파일은1927경고/오류0, 이후 최신 증분 로그 `consumer-build-preview-fixture.log`는45경고/오류0이다. 경고 수는 실행 범위를 구분하며 무경고라고 하지 않는다. 출력은 기존 `Temp/bin/Debug`·`Temp/obj`, Assets 안 bin/obj 생성0이다.
- .NET 기존 관련 시험은 `sensory-contracts.trx`5/5와 `farm-plan-contracts.trx`109/109, 합114/114 통과다. 신규 Unity13사례는 컴파일만/실행0이다. 전체 Hub 시험은 기존 Scene을 열기 때문에 이 좁은 범위로 실행하지 않았다.

공간의 `observation-probe.cs` SHA `2F8227CEE65088D71804E0C92D890E6497BBC34C80F423E873B3BB737B872CE6`와 `input-plan.json` SHA `537AC9137B2A79AEE6D25511C30D3CB4E60DE11E3C0D799A7DBD18FE28BFDE8F`를 전체 읽었다. 명시26DTO/단일 직렬참조·현재셀 식별요약, null 미관측·Provider/Raycast 제외 계약을 유지했다. 원 plan의 준비명세7AC916/후속69ED9는 당시 판본이며 최종 실행명세로 소급 치환하지 않는다. Probe 오프라인 컴파일은 성공(응답파일 `/noconfig` 관련 CS2023 경고1); 기준선/로더 wrapper 컴파일은 경고/오류0이며 실제 Pipeline API 호출 성공은 미검증이다.

개발 관찰 기준선은 객체 TRS/활성/부모·신원/선택·Preview/Graph요약만 비교하며 전체 컴포넌트/캐시 보존이 아니다. 공간 정적검토에서 Preview Scene 누락·출력 한도 검사 시점·최종 원문 회수 실패 경계3건을 보완했다. 현재 `d422ObservationExecution`은39파일·정확 slot-01·baseline8MiB/전체32MiB·Compile1/Observe1 및 eval별 별도 메모리Assembly 관문을 담고 실행 승인/시작통보 전 검토 중이다. 새 셀/Provider·Prefab 조회/조립/Play/보행·캡처·Scene저장은 아직0이다. 실제 실행 결과와 소스 편집에 따른 Editor 자동 import 여부는 현재 미관측으로 남긴다.

## D422 Farm 한 셀의 배치 소비 준비

[추가 승인](../AI/Farm월드맵-배치계층경유-점진구성-2026-08-31.md) `farm-world-map-placement-layer.plan.r1` SHA `C4672FB891BB699CFEE95DBB52AA1C85DA56AA3E7CC09E97852A313993BB966A`를 전체 읽고 기존 Farm 기획/공간 r1·r2/LS01 Accepted 네 hash의 현재 일치를 확인했다. 기존 후보·연구와 위 과거 시험/실행 기록은 변경하지 않는다. 새 조립 승인 전체를 준비 완료로 해석하지 않고 [Farm 명세](../../eng/execution-ledgers/work-orders/farm-crop-cycle.e7-work-order.json)의 `landscapeConsumptionBinding.d422Preparation`과 기존 두 지원 작업에 정확 경계를 연결한다.

### 첫 연결과 소유

- 개발: 기존 명세/Goal·이 보고/D406 인계 후속절·CURRENT/통합판 및 공식 Goal 생성물. `artifacts/local/validation/farm-world-map-d422/preparation-01/`에 수정 전 7파일 사본과 총27입력 기준선을 남겼다. 필요한 게임/Unity 코드 경로는 실제 관찰 계약 뒤 별도 좁게 추가한다. 현재 Runtime 변경 배분은 없다.
- 공간: 기존 `Farm경관LS01실측Probe.cs`와 D375/D406 도구의 파일읽기 반환을 인수했다. Unity `artifacts/local/validation/farm-world-map-d422/observation-01/observation-probe.cs`와 `input-plan.json` 두 파일 준비를 명세 SHA `7AC916C749668F6F6979CE00924CBB174F262522B68F7AD03485C8EBB07A8202`에 결속해 배분했다. `Observe(string inputPlanJson)`의 명시 DTO 투영만 사용하며 임의 셀 선택·초기화·Sessions.GetAsync·범용 rawPlan 직렬화·이동 Gate 호출을 하지 않는다. 아직 Editor/Compile/Run 허가는 없으며 단독 실행의 상태/출력/보호 hash는 준비 뒤 별도로 동결한다.
- 첫 순서는 실제 한 셀/H계보·지면/보호/통로 사본 → 단일 Prefab 키 해결 → 기존 변환/적합성/소비 검사 → Partition → 기존 Coordinator의 기대 ID·변환 보정·누락/경고·소유해제 대조다. 13객체는 원A/B 고정변환의 비교 단위이며 첫 작은 실제 배치의 무조건 필수 수가 아니다.

### 현재 파일 대조에서 확인한 책임과 차단

`배치통제계층Root.cs`의 v1~v4는 유지한다. H공간배치규칙EditorEngine은 v1, Hub창고정밀배치Builder는 v4를 생성하는 코드가 있다. Root.Validate는 구조/판본/부모·Player/비율/상향 단계/감각 채널을 검사한다. 별도 H공간배치규칙증거Root는 SyntheticFlatH3Underlay·4영역·100객체 이상을 요구하는 기존 실증 전용 검사이므로 한 Farm 셀에 전체 조건을 강제하지 않는다. 이 코드 읽기는 현재 저장Scene 인스턴스 판본이나 실제 통행 시험이 아니다.

기존 `SimulationFarm경관고정배치Service.Convert`와 `PartitionFrozen`은 원A/B 및 제공표면을 검사하지만 계보를 만들어 주지 않는다. 현재 Unity ResolvePrefab은 Nature 직접키 또는 CompositionCatalog를 쓰며 `Assets/Ssalddel/**/*.asset`의 `candidate:ls01:single-prefab` 등록은 검색에서 찾지 못했다. 동적 등록의 전역 부재 증명은 아니다. Runtime의 자동 가시바닥 정렬과 Ambient 누락 건너뛰기를 고려해 Apply/셀Active와 실제 기대객체·고정TRS 성공을 구분해야 한다.

D375 당시의 Farm 셀/원점/H4 차이·Provider와 Collider 높이 차이는 재확인 대상이지 현재값이 아니다. 빈 보호/통로 목록·기본 H 문자열·합성높이를 실제 사본으로 대신하지 않는다. 현재 정확한 새 후보/실제 지면·통행/Renderer·Scene 적용은 미검증이다. 새 배치 선택이 필요한 부분은 원연구를 보존한 후속 연구의 Accepted 기준선으로 연결하고, 기술 연결은 D422 범위에서 진행한다.

진행 상태: 준비 명세 결속/전문 외부2파일 작성 배분. 명세 Validate와 Goal r140 Write/Check 통과, Fast 계획은 GuidanceOnly/BuildTargets·TestPlans 빈 값이다. D419~421의 동결4원문·검색 입력은 미변경이며 D422 sources 등록은 보류한다. D423 승인 `1C0FBAC3AA377F1096FF398CAFB096CEE036A90CBA08EC1D52A195447B64A5CE`와 책임 기준 두 Architecture의 `46BB377E…776E`/`84937331…7512`를 읽고 대조했으며 원문은 쓰지 않는다. Root.Validate의 내부 선언검사 분리와 Scene열기 없는 회귀의 정확 경로를 검토 중이다. 현재 새 게임코드·시험/Editor/Play/Scene저장·보행·캡처/E 승격0이며 이후 결과는 이 절 아래에 추가한다.
