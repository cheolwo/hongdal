# D439 기획 문서의 로컬 MySQL 정리 — 구현 결과

## 범위

[기준·승인](../Architecture/기획판본-서버반입파이프라인.md)의 API 비필수 정정을 반영했다. 기획 문서를 로컬에서 읽고 기존 ASP.NET Core 저장 기능을 직접 호출한다. 새 HTTP Controller/큐/데몬·서버 Context/DI·DDL을 만들거나 기존 게임 규칙을 바꾸지 않았다.

- [문서 검사·판본 도구](../../eng/planning-inquiries/manage-planning-release.ps1): 일곱 칸·WI 판단·객체/참여/시각 관계와 정확 원문/hash를 검사한다. 같은 ID/판본의 변경 거부, 재생성 멱등, 현재 문서와 생성 사본 대조를 제공한다.
- [로컬 실행기](../../eng/planning-inquiries/local-import/Program.cs), [프로젝트](../../eng/planning-inquiries/local-import/PlanningLocalImport.csproj): 기존 WI 참여 UseCase의 같은 프로세스 호출. HTTP0, 기존 정의 참조, 로컬 전용 DB 확인·preview/apply·독립 EF 조회.
- [회귀시험](../../eng/tests/planning-release.ps1): 실제 DB에 쓰지 않는 원문/참조/판본·미지원 입력/로컬 실행기 차단 시험. [사용법](../../eng/planning-inquiries/README.md#d439-기획-문서의-로컬-mysql-정리)을 추가했다.
- [첫 실제 기획 문서](../AI/기획판본-D439-기존벌목관계-첫로컬반입.md): 기존 WI-NATURE-06과 이미 등록된 design:wi:tree-resource의 Target 관계 하나. 새 WI·객체 정의·Prefab 선정이 아니다.

## 검증 및 실제 반입 상태

**기존 벌목 WI와 나무 정의의 참여1건을 로컬 MySQL에 저장·독립 조회했다. API 호출0이다.** D437이 DB 보호/쓰기 슬롯을 반환한 뒤 D439가 단독 실행하고 종료·슬롯 반환했다. 해당 결과를 D437의 기존 보호 시점으로 소급하지 않는다.

| 확인 | 실제 결과 |
| --- | --- |
| 문서 도구/로컬 실행기 차단 회귀 | 60/60. 실제 DB 미사용 시험 |
| 실행기 및 서버 참조 빌드 | 처음 전체 빌드0오류·기존 서버 nullable2경고. 실행기 최종 수정 증분 빌드0경고/0오류 |
| DB 읽기 preview | 기존 정의1 재사용, 예정 관계1, 같은 문맥 기존 관계0 |
| 최초 apply | 기존 UseCase `Persisted`, 신규 정의0·참여1·추출묶음1 |
| 같은 입력 repeat | `Duplicate=true`, 추가 정의0·참여0 |
| 독립 확인 | 새로운 EF Context의1행·입력hash·WI/대상/역할·구성판본 참조 일치, 재사용 정의hash 불변 |
| 제외 | DDL/새 정의/자산 선택/실제 World·Unity·게임 실행0 |

대상은 `hongdal-mysql-1` / `127.0.0.1:13306` / `hongdal_dev`다. 명시 로컬 유지보수 주체와 기존 관리자 정책을 사용했으며 앱 로그인·설정/DI를 변경하지 않았다. 비밀값/전체 docker 환경은 저장하거나 출력하지 않았다.

검증 근거:

- 저장용 사본 `artifacts/local/planning-releases/CC5ECAD48806D50FB181A0F8ACEA1975CF12BB3ACC5E55C55427E196658BE332.json`, SHA `27216FFB316F99348DB7A09B711CC769665826685041EB102E01D2C360364A7F`.
- 실제 `artifacts/local/validation/planning-release-d439/local-first/preview/result.json` 및 `apply/result.json`, 각 별도 claim. 원문은 `Prepared_NotSubmitted` 사본으로 보존하고 실제 저장 성공은 이 실행 결과와 구분한다.
- 기본52시험 뒤 실행기 저장 전 거부8사례를 더해60사례다. 중복 합산하지 않는다. 개별 결과는 `artifacts/local/validation/planning-release-d439/tests-*/results.json`에 남는다.
- 범위11경로 Fast/Task `20260831-205306`은 도구 경로를 GuidanceOnly로 분류해 diff만 검사했다. 별도 실행한 실제 .NET 빌드/60회귀를 대신하지 않으며 제품 전체 Task 성공으로 표시하지 않는다.

최초 DB 읽기3회는 실행기 필터의 배열 `Contains` 표현 해석에서 `InvalidOperationException`/내부 `ArgumentException`으로 중단됐다. 저장 시도0이며 진단을 보완해 실행기106행으로 좁힌 뒤 `Enumerable.Contains`를 명시했다. 수정 후 실제 preview/apply·독립 조회가 통과했다. 원문/DB 조건을 완화하거나 실패를 샘플 성공으로 바꾸지 않았다.

## 남은 범위

첫 로컬 저장은 기존 WI 재사용/기존 객체/직접 WI 필드 근거만 지원한다. 새로운 WI·객체·자산 구성과 선정, Markdown 자체만의 참여 근거는 문서에 표현 가능하지만 실제 저장 준비 미완료로 명시한다. 지원분만 암묵적으로 저장하지 않는다.

일곱 칸은 기존 관계 ContextNote 안에 구조화하여 출처와 함께 보존하고 WI/정의/역할은 기존 RDB 열을 재사용한다. 일곱 칸의 별도 SQL 인덱스는 없다. 의미 승인·권리·시각 적합성·실제 World/E 승격 증거와 구분한다. r129/야간/Unity/Scene·운영원천·commit/push 변경 없음.
