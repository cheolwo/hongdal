# 누적 변경 D448 — 개발 정합화와 검증 인계

- 범위: 기존 누적 변경의 코드·검색·관리 정합화와 시험. 새 게임 기능, E 승격, 실제 Unity 화면, Provider 호출이나 DB 변경의 완료 보고가 아니다.
- 시작 Hongdal HEAD: `712c4a08349bcda0b7c4b0489bfb8ad2a1e7087a`, dirty 257경로, staged 0. 다른 담당 변경을 보존했다.
- 기획·Git 소유: [D448 조율 보고](전반리팩토링과맥락별커밋-2026-08-31.md). 개발은 stage/commit/push하지 않으며 최종 파일 해시와 미통과 항목을 반환한다.
- 상세 로그: `artifacts/local/validation/refactor-commit-20260831/development-validation/`. 아래 시험은 합쳐진 작업 트리에서 실행했으며 각 중간 커밋에서 독립 실행한 결과가 아니다.
- 작업은 8월31일 시작했고 최종 검증은 9월1일로 이어졌다. 요청된 보고서 파일명과 선행 실행시각은 소급 변경하지 않는다.

## 1. 정합화한 범위

| 묶음 | 실제 변경 / 보존 |
| --- | --- |
| 문답·공간 관계 | 기존 다영역 2근거를 r2, 시점 역할 6근거를 r3의 실제 hash/위치에 결속했다. D428의 Town 참여 Confirmed와 Recipe·시간·비용 미정을 구별했다. 과거 승인 원문·옛 실행 증거 hash는 바꾸지 않았다. |
| 생성 주제 제목 | `spatial-query.ps1`의 OrderedDictionary를 `topicCode` scriptblock으로 묶도록 수정했다. 빈 제목 거부와 원래 주제 제목 회귀를 추가했다. 새 주제/질문 의미를 만들지 않았다. |
| 검색 회귀 | 현행 477질문/909발췌/486입력, 원문 대조 474·미검토 3을 분리했다. 새 3의미 ID는 순환 의미를 추정하여 채우지 않았다. 기존 검색 README의 D416 당시 Asked 안내는 D428 현행과 역사로 구분했다. |
| 추가 문답 실행 추적 | 전체 검색과 달리 선언된 Q340~403 64개+명시 의미 ID 34개=98개만 평가한다. 선언 ID 존재·중복·정확 행집합·근거 hash를 확인한다. 원천 증가를 40개 평가행 자동등록이나 새 실행 승인으로 바꾸지 않는다. Q397 근거만 현행 첫플레이 r10/hash에 재결속했다. revision2값/hash2값을 역치환한 전체 바이트 hash가 시작 `8364C09E1BF830F10B97ED5B3FFB9832CC7C8A47E02915BEE1A6ACC5FA5A4610`와 같아 기존 판단/WI/E 불변을 확인했다. |
| 두 구판 명세 | 외부 사본에서 r5 검증을 먼저 통과한 뒤 기획의 추가 승인으로 Actor 물품/장착과 Nature 전술 이동 원명세의 `protocolRevision` 한 필드만 r4→r5 변경했다. 역치환 바이트 hash로 다른 내용 불변을 검사한다. 기존 E7 값은 이번에 올린 값이 아니다. 명시 이전 hash를 참조하는 실행 원장 행은 발견되지 않아 과거 증거를 치환하지 않았다. |
| 시각자산 hash 검사 | `보유시각자산목록UseCase.Hash`가 기존 `시각자산근거검사.Hash`를 소비하도록 중복을 제거했다. 잘못된 hash의 거부·반복 결과·입력/저장 불변 회귀를 추가하고 기존 진단 코드를 보존했다. |
| 분류 DB 스키마 | 신규 `개체시각대응SchemaTests`는 실제 연결 없이 MySQL 모델 DDL을 생성한다. PK·Snapshot FK/Restrict·3열 고유 인덱스를 검사한다. DDL 파일은 `ModelGenerated_NotApplied`이며 실행하지 않았다. |

수정 source/test는 `eng/planning-inquiries/{spatial-relations.json,spatial-query.ps1,README.md}`, `eng/tests/{planning-inquiry-search,planning-inquiry-spatial-search,playable-loop-inquiry-implementation-scope}.ps1`, `eng/execution-ledgers/{manage-playable-loop-inquiry-implementation-scope.ps1,playable-loop-inquiry-implementation-scope.json}`, `work-orders/{actor-item-equipment,nature-tactical-self-navigation}.e7-work-order.json`, `Ssalddel/Services/Content/보유시각자산목록UseCase.cs`, `Ssalddel.Tests/Services/Content/{보유시각자산목록Tests,시각자동할당Tests,개체시각대응SchemaTests}.cs`이다. 검색·범위·Goal·표현·코드/E책임 지도는 기존 생성기로 처리하고 생성물을 직접 편집하지 않았다.

## 2. 실행 검증

| 검증 | 결과 / 근거 |
| --- | --- |
| Content 집중 Fast | 349/349, `20260831-234236/Ssalddel.Tests.trx` |
| Simulation Task | 1,604/1,604, `20260831-234649/Ssalddel.Simulation.Tests.trx`; 빌드·지도 검사 통과 |
| Unity 독립 .NET Task | 697/697, `20260831-234746/Ssalddel.Unity.Tests.trx`; 실제 Unity Editor/Play 시험은 아님 |
| 서버 전체 Task | 4,855/4,862, skip 0. `20260831-235440/Ssalddel.Tests.trx`; `Ssalddel.v0.0.slnx` 빌드는 통과, 기존 타영역 7실패로 전체 Task 미통과 |
| 기획 판본 로컬 반입 소비자 | `eng/planning-inquiries/local-import/PlanningLocalImport.csproj` build 0경고/0오류, 실제 반입 실행 0 |
| Goal·Presentation | Write/Check 또는 Validate 및 두 회귀 통과. Presentation 42사례. `Management-20260831-235355` |
| E7·추가 문답 추적 | E7 회귀 통과, 범위 Write/Validate 및 기존 339+추가98 회귀 통과. 추가추적 28검사, 기존 보호파일 불변 |
| 코드/E책임 지도 | 기존 생성기 Write→Check 모두 통과. `Maps-20260831-235528`; 지도 diff에는 기존 다른 작업 반영도 있어 전부 D448 신규 코드로 세지 않는다. |
| 전체 개발시스템 관리자 | 미통과: 아래 방문자 과거 증거의 `EvidenceArtifactHashMismatch`. 첫 차단 뒤 후속 모든 관문 통과를 주장하지 않는다. |
| 검색·공간·WI·릴리스 회귀 | 일반 문답64·공간115·WI22·기획릴리스57 통과. 공간 최종 로그는 `spatial-final-null-proof.log`이며 새 미검토 행의 null proof를 읽던 시험 필터1행을 보완했다. 기존 source 보호4개도 동일하다. 실제 JSON+Markdown3의 명시 Write/Validate·빈제목0은 별도로 통과했다. |

서버 7실패를 보존된 `20260831-205049/Ssalddel.Tests.trx`와 직접 대조했다. 이름과 오류 메시지가 7/7 정확히 같았다. 따라서 선행 실행에서도 관측한 실패이지만 모든 변경과의 인과적 무관함까지 증명한 것은 아니다. 이번 범위 밖 Controller/UI를 임의 수정하거나 실패를 제외해 전체 통과로 바꾸지 않았다. 과거 4,831/4,838 결과와 이번 4,855/4,862는 별도 실행이다.

| 실패 시험 | 이번 첫 오류 / 미충족 값 |
| --- | --- |
| `Controllers_HaveIntroductionHistoryMetadata` | 지도신청운송취소검토Admin·신청개인정보동의·지도신청가원장 Controller의 도입 이력 미충족 |
| `통합_WebApp의_모든_컴파일된_라우트는_capability_규칙으로_분류된다` | `/community/map-application-chooser` capability 규칙 없음 |
| `RoleAppControllers_HaveAudienceAndBusinessCapability` 주문자 | `주거공동체World관점Controller` 한국어 업무 영역 없음 |
| 같은 시험 기사 | `기사주거공동체World관점Controller` 한국어 업무 영역 없음 |
| 같은 시험 화주 | `농장생산자World관점Controller`의 audience `화주·판매자`에 요구값 `화주` 없음 |
| `CommonAndAdminDomainControllerActions_UseKoreanOrApprovedTechnicalPrefixes` | `농수산정보Controller.Hs식품국가가격Card조회` 동작 명명 규칙 미충족 |
| `공식재료화면은_재료이름과_좁은폭동작영역을_실제값으로연결한다` | CSS의 `min-height: 44px` 미확인 |

이번 직접 source 변경은 시각자산 hash 검사와 검색/관리 경계이며 위 Controller/UI를 수정하지 않았다. 상세 원문 오류 및 선행/현재 TRX hash는 최종 `test-results.json`/`prior-failure-comparison.json`으로 반환한다.

초기 검색 `SpatialProofDrift`, 이전 개수/미검토0 고정 회귀 실패, 초기 범위 `AdditionalSourceCoverageChanged`와 구판 protocol 문제 로그도 보존했다. 수정 전 실패를 최종 성공 로그로 덮어쓰지 않았다. 초기 orchestration의 Write/Validate는 JSON만 대상으로 해 기존 Markdown3이 남았다. 기획의 대조 후 기존3출력 경로를 명시하여 재생성했고 `four-views-validate.log`의 실제4출력 검사와 빈제목0을 확인했다. 첫 명시Write는 생성 완료 뒤 wrapper 인용조건식 오류로 exit1이었고, 별도 Validate exit0으로 생성물 결과를 확인했다. 옵션 기본동작을 넓히거나 생성물을 직접 수정하지 않았다.

최종 감사 보조스크립트의 첫 실행은 git 경고 ErrorRecord를 그대로 JSON으로 직렬화하다 깊이 경고가 발생해 정확 자기 감사 프로세스만 종료했다. 이미 생성된 작은 감사파일7개는 `pre-final-audit`에 보존했다. 경고를 문자열로 수집하도록 보조스크립트만 보완하고 최종감사를 별도폴더로 수행한다. 제품/시험/Editor 프로세스를 강제종료한 것이 아니다.

## 3. 남은 두 실제 정합성 문제

### 방문자 과거 E4 근거

- 패키지: `evidence:nature-camp-visitor-stay-e4-20260830`, `eng/execution-ledgers/evidence-packages.json`.
- 파일: `Ssalddel.Unity/Runtime/Cards/Simulation방문자체류PresentationPreparation.cs`.
- 등록 hash: `3EA07C3D84C39E382AD093E3B38402D2F4BF1DDE3332E1200D5A79F13A37EE54`.
- 현재 hash: `DF1E43495CF02997BB12B57DEEE2EAB3E18331B5A75D0E17B522BD72A8088E4B`.
- 누적 코드에는 상태별 VisualKey/AnimationRole/ActionCue를 확인하고 기존 Project를 소비하는 `ProjectWithStateBindingValidation`이 추가돼 있다. 이는 과거 E4 7시험·원판본과 다른 현재 코드다.
- 현행 독립 .NET 697통과는 과거 E4 패키지의 당시 환경·자산/연구·판본 전체 재입증이 아니다. 기획과 조율하여 이번에는 과거 hash·status/E를 수정하지 않고 관리검사 차단으로 반환한다. 정식 근거 재검증/판본화가 후속이다.

### 실제 분류 테이블 고유 인덱스

- 허용된 읽기만 수행: `hongdal-mysql-1/hongdal_dev`, `information_schema.statistics`의 `world_visual_inventory_classifications` 인덱스 메타데이터.
- 실제 관측: `AnnotationId` PRIMARY 및 `SnapshotId` 비고유 FK 보조 인덱스만 확인. 모델이 요구하는 `(SnapshotId, TaxonomyHash, TaxonomyPath)` 고유 인덱스는 없다.
- `development-schema/actual-index-observation.json`에 기록했다. 모델 DDL SHA `E9CEF12F46203D51326E9C6C33DFAC78FF761E88D6903E007D5E22AD4D7A471F`; 독립 모델 시험 두 실행에서 동일했다.
- 새 DDL·데이터 행 조회/수정·Provider/API/키 실행은 0이다. 기존 저장·멱등 시험과 실제 DB 경합 보장은 구분한다. 중복 여부/보완 DDL·적용 검증은 별도 후속 승인 범위다.

## 4. 미통합으로 남았던 D440·D442/D443의 실제 범위

**D440:** `artifacts/local/validation/game-object-auto-assignment-d440/apply-result.json` 원결과를 다시 읽었다. 기존 사본 14,461/Prefab 4,221 중 catalog 대응 4,211·재고만 10, 분류 관계 5,346 저장·재처리 추가0·독립 5,346행 일치였다. 분류된 Prefab 3,684/미분류537/보조10,240이며 의미 적합성 완료는0이다. 이번에는 DB 저장을 재실행하지 않았다. `assetSelections=0`, `worldInstances=0`이고 Vessel/Display/BuildingExterior/Signage/WorkEquipment 5역할은 근거 부족으로 보류다. 작은 냄비를 양조 용기로 바꿔 선정하지 않았다. 자동 할당 기능의 코드와 실제 후보 채택은 다른 결과다.

**D442/D443:** [격리 검토 연구](../Architecture/PlayableLoops/패턴조합-D442-격리검토연구.md) `58A1C3DB059413D4629099CB441BE10949C6334802B3FFABE03A1227F76ED041`는 `AcceptedPreparation`이다. D443의 63개 선언 후보(단독9/쌍27/삼중27)는 전부 미실측·Incomplete이며 `declaration-candidates-d443.json` SHA `77B055A04848E5F2BC17A9BB96133D210D12D45B2C462682C19F15E127F975E7`을 보존한다. 기존 회귀48 및 집중157/전체1,604 이력과 이번 Simulation 전체1,604를 구별한다. 모판/실배치·지지·연결부 치수·Preview/PNG는 미확보다. 과거 이름의 미작성 통합 보고서를 새로 만들어 이미 완료됐던 것처럼 표시하지 않고 이 절에 준비/미완료를 통합한다.

## 5. Unity 별도 저장소와 반환 경계

개발은 `LocalNpcDialogue/Directory.Build.props.meta` 빈 필드 3곳의 행말 공백만 제거하고 GUID `70b4d3e5a4259f74aa54b2f2ad3e5dbe`를 유지했다. Core 32/32·vendor 9/9, Compatibility build 0경고/0오류는 독립 .NET 검증이며 Editor/LLM 모델 실행이 아니다. 출력은 Assets 밖 경로를 사용했다. Blender 4파일은 기존 환경 준비 자료이며 실제 모델·Clip 제작이 아니다.

기획 담당은 별도 저장소의 정확 6파일을 `3015893d`(출력 격리 props/meta2), `094f225d`(Blender 환경4)로 커밋하고 push0이라고 인계했다. 이는 기획 담당의 Git 결과이며 개발이 재커밋하지 않았다. 나머지 Unity 655경로는 기획의 시작hash 대조 보존 반환으로 구분한다. 개발의 이번 전체 Unity 전수 재검사·Editor 조작을 뜻하지 않는다.

기획은 Hongdal에서도 검증된 소스·기획·자료174경로를 8커밋으로 분리했고 HEAD `daa13581`이라고 중간 인계했다. 개발은 해당 소스 쓰기를 종료했고 남은 조회/관리·통합 문서만 마감한다. HEAD 변경만으로 소스 바이트가 달라진 것으로 간주해 같은 시험을 반복하지 않았다. 전체 커밋 목록·잔여 경로·기존 행말공백 예외의 최종 판정은 기획 소유 조율 보고를 따른다.

현재 E/Goal 활성 상태·기획 승인 범위·Save·Scene·자산 원본·r129/야간 중지를 유지한다. 코드/문서 최종 해시와 검사 결과는 별도 local manifest로 반환하며, 기획은 명시된 실패·미검증을 포함해 맥락별 커밋을 판단한다. 운영 배포·Unity 전체완료·모든 누적 변경 승인으로 확대하지 않는다.

## 6. 최종 개발 반환

지정 보완·검증·부분결과 문서화는 마감한다. 전체 통과가 아닌 **알려진 미통과를 포함한 개발 인계**다. 남은 기술 조율은 서버7실패, 방문자 과거E4 근거의 정식 재검증/판본화, 분류 DB 고유 인덱스 보완이다. 이번 범위에서 이들을 우회·자동수리하지 않았다. 실제 게임 연결·시각·E 결과는 각 기존 승인 작업의 후속이며 자동재개하지 않는다.

최종 인계 폴더는 `artifacts/local/validation/refactor-commit-20260831/development-validation/final-audit/`이다. `development-files.json`은 직접 개발22경로의 최종 바이트hash, `work-order-single-field.json`/`scope-proof-only.json`은 승인된 좁은 변경의 역치환 대조, `test-results.json`/`prior-failure-comparison.json`은 TRX와 실제 선행실패 대조, `protected-planning.json`은 기획 동결3원문 대조다. `hongdal-after.json`은 진행 중인 기획 커밋을 구분하기 위해 현재dirty 수와 시작대상 포함 관측경로 수를 별도로 기록한다. `manifest.json`은 자기 제외 감사파일을 식별한다. 3문서의 로컬 경로303개 존재와 지정 Fast는 전체 링크의 의미·앵커·게임실행 검증과 구분한다.
