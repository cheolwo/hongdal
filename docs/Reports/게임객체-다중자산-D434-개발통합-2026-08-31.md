# 게임 객체 역할별 자산 구성 — D434 개발 통합

- 승인: [D434 원문](../AI/게임객체-다중자산구성과공방-개발인계-2026-08-31.md), SHA256 `A6B25D49AEF0F610007AB4DC28A5F497C641155E5E272DA165F23DEC5C1620D9` 전체 읽기·일치.
- 결과: 코드/독립 시험 및 로컬 MySQL 편집 초안 저장·독립 재조회 완료. **공방 자산 선정 0, 실제 World/Session/Scene·배치·게임 기능 0**. 적합 후보를 만들거나 기존 상점/감자상자로 채우지 않았다.
- 근거: `artifacts/local/validation/game-object-multi-asset-d434/`. D431 과거 결과/승인 SQL/실제 기록은 보존한다.

## 1. 재사용과 경계

기존 `SimulationFacilityBlueprintRequest`는 건설 재료/기간/지구·배치 조건, `SpatialCompositionRule`은 H 자식/능력/성립 규칙을 다룬다. 이들을 편집용 Prefab 목록으로 바꾸지 않았다. 조사한 Simulation Contracts/Application·Domain·world-seedbeds에서 NPC 양조 공방의 그대로 쓸 편집 정의는 찾지 못했다. 이는 저장소 전체 부재 증명이 아니다.

D431 자산판본, 기존 전용 DbContext, 관리자 정책/현재 사용자, 검토 스위치와 hash 규칙을 재사용한다. 기존 단일 대응의 ContextHash unique/BindingConflict/개별 우선/승인 선택을 변경하지 않았다. 구성 저장/조회는 별도 `compositions` 하위 경로이며 Purpose를 복제하지 않는다.

## 2. 코드와 관계

| 경로 | 책임 |
| --- | --- |
| [게임객체시각구성.cs](../../Ssalddel.Domain/Content/게임객체시각구성.cs) | 편집 정의 포인터/불변 구성판본/역할 항목/요청 이력 |
| [구성 계약](../../Ssalddel.Contracts/Admin/Content/게임객체시각구성Contracts.cs) | 요청/응답, 미선정·후보·이미지 미관측 구분 |
| [기존 DbContext](../../Ssalddel.Infrastructure/Persistence/WorldProjection/개체시각대응DbContext.cs) | 기존 3표 유지 + 새 4표, 자산판본 FK·삭제 제한 |
| [구성 UseCase](../../Ssalddel/Services/Content/게임객체시각구성UseCase.cs) | 관리자/주체 일치, 판본/멱등, 원자 저장, 사본과 FK 행 검산 |
| [기존 관리자 Controller](../../Ssalddel/Controllers/Admin/Content/개체시각대응Controller.cs) | POST/GET compositions 및 GET compositions/{definitionId}?revision= |
| [DI](../../Ssalddel/Extensions/ServiceCollectionExtensions.ApplicationCore.cs) | scoped UseCase 등록 |
| [구성 시험](../../Ssalddel.Tests/Services/Content/게임객체시각구성Tests.cs) | 관계/미선정/반복사용·경합·롤백·변조 |
| [기존 회귀](../../Ssalddel.Tests/Services/Content/개체시각대응Tests.cs), [HTTP 회귀](../../Ssalddel.Tests/Services/Content/개체시각대응HttpTests.cs) | 단일 대응 유지, 7표 모델 검사, 실제 테스트 서버 인증/정책 |

추가 테이블은 `world_visual_object_definitions`, `world_visual_object_compositions`, `world_visual_object_composition_items`, `world_visual_object_composition_history`다. 정의의 현재 revision 포인터와 새 판본·역할·이력을 한 SaveChanges 트랜잭션에 저장한다. 이전 판본은 덮어쓰지 않고 특정 revision으로 조회한다. 항목 ID 중복과 같은 역할/슬롯 중복은 거부하고, 서로 다른 슬롯의 같은 역할·동일 자산 반복과 다른 정의의 재사용은 허용한다.

정의/구성 상태는 항상 `EditableDefinition_NotWorldInstance` / `Draft` / `NotApplied`. 자산이 연결되어도 `Candidate_FitnessUnreviewed`; 이미지 연결은 아직 `NotObserved`. 증거 경로/hash는 검토자가 선언한 사본으로 보존되며 이 API는 임의 파일을 읽거나 원문 의미/신선도를 인증하지 않는다. 자동 승인/적용 API는 없다. 미선정 위치를 원점으로 만들지 않으며 AnchorIntent는 선택적 설명이다. 실제 NPC/시설 인스턴스나 건설 Blueprint/H 등록은 없다.

## 3. 실제 로컬 MySQL

- 기존 container `hongdal-mysql-1`, ID `009d54650abf094cd55489f1a456d9ebe23322c00d672ebf74d34c84f17ce78b`, Compose `hongdal` 개발 의존 설정, `127.0.0.1:13306/hongdal_dev` 재확인. 기존 non-root 자격정보는 메모리로만 사용했고 보고/인수 파일에 넣지 않았다.
- `runner/approved-schema.mysql.sql` SHA `5DD96A83BDADDCCD5F423DD49A2641EDA00F0A95B1A6FE7330AB9BAA3EC4DAF8`: 4 CREATE TABLE + 4 INDEX만. 전체 Migrate/EnsureCreated/ALTER DATABASE/기존 테이블 ALTER·초기화/마이그레이션 이력 변경 없음. DDL은 공급자 특성상 전체 원자 rollback을 보장하지 않아 claim/부분 결과를 보존한다.
- 실행 주체는 D434 명시적 로컬 유지보수 principal `local-maintenance:d434`. 기존 앱 로그인/사용자 계정 위장이 아니다. 실제 배포 앱의 HTTP 또는 다른 DB 연결 복구/설정 변경은 수행하지 않았다.
- `design:town:npc-brewing-workshop` / `d434.r1`: 정의 1, 구성판본 1, 역할 5(외관/작업대/용기/진열/간판), 이력 1, 선택된 자산 **0**. 역할은 편집 예시이고 전부 필수 설비나 수량/배치 승인표가 아니다.
- 같은 입력 재처리 Duplicate=true, 행 추가 없음. 새 EF 문맥의 DTO와 SQL JOIN 독립 조회 일치. `save-result.json` 상태 `EditableDraftPersistedAndRead_Unselected_NotWorldInstance`.
- 원천 9표 행 수·공개 식품 identity hash 및 D431 전용 3표 전체 행 hash의 전후 일치. 모든 비공개 원천 레코드 전체 hash 검증은 아니다. D431 자산8/대응3/이력3 불변.

## 4. 검증 및 한계

`tests/d434-focused.trx`: 초기 98/100. 추적 중 optional FK 자동 null 처리와 경합 오류 분류 2건을 실제 실패로 보존했다. 새 항목의 자산 FK에 ClientNoAction을 사용해 삭제 시 null 자동 변경을 막고, 공급자가 UNIQUE 경합을 먼저 반환하면 현재 revision을 다시 읽어 RevisionConflict로 구분하도록 보완했다.

`tests/d434-focused-r2.trx`: **100/100, skip0**. 기존 77 + 새 구성17/HTTP6. 다중 항목/동일 자산 반복/다른 객체 재사용, 권한4상태, 고아 자산·삭제 제한, 항목/역할슬롯 중복, 개정·이력·멱등·오래된 추적 문맥, 이력 저장실패 전체 rollback, 사본/항목 변조 및 기존 단일 대응 호환을 검사했다. SQLite 합성 자산 입력은 실제 Prefab 적합성 증거가 아니다.

첫 시험에서 기존 D431 `schema-test/mapping-schema.mysql.sql`이라는 파생 모델 출력 경로가 재사용됐다. 후속 시험은 D434 전용 schema-test로 정정했다. D431 실제 적용 `runner/approved-schema.mysql.sql` 및 입력/결과/DB 행은 변경하지 않았다. 범위 Fast의 최종 결과는 아래 후속 검증란에 기록한다. 기존 서버 nullable 경고 2곳은 별개다.

## 5. 남은 실제 연결

### 후속 검증 결과

- 범위 Fast `artifacts/local/validation/20260831-201111`: `Ssalddel.v3.5.slnx` 빌드 및 관련100시험 통과.
- Task `20260831-201231`: 같은 솔루션 빌드 통과, 서버 전체4,794 중4,787 통과/7실패/skip0. 범위 밖 실패는 지도 신청 선택 라우트 capability, 주문자/기사/화주 Controller 분류3개, Hs식품 동작 명명, 공식재료 CSS 최소높이이며 임의 수정하지 않았다. API 도입 메타데이터 검사에는 현재 Controller와 다른3개도 포함됐다.
- 현재 Controller의 도입 버전 이력(V3_5, 제품 기능 활성화/E 승격 아님)을 보완하고 새 구성 동작은 한국어로 명명했다. `tests/d434-final.trx` 관련 **101/101** 통과. 전체 suite 7실패를 전체통과로 바꾸어 보고하지 않는다. 다른 Controller 도입 누락은 해당 소유 후속이다.
- 이어지는 D435의 참여2표/시험은 D434 당시7표와 별도다. D434 적용 SQL/실DB 결과는 그 당시 판본으로 보존한다.

공방 적합 자산은 미선정이다. D433 전담 조사 인계의 구매/적용 라이선스 미확인·업로드 보류를 유지하며 새 전문/Editor 캡처를 중복 배분하지 않았다. 후보 비교 UI/이미지 DB 관계·Azure 인증 웹 표시·승인 구성의 실제 적용은 미구현/미검증이다. 다중 관계 자체를 실제 asset8 대량 결속으로 보여주지 않는다.

D435의 WI→게임 객체 추출/참여 관계는 이 구성의 시각 부품과 별도 후속이다. 해당 원문을 전체 읽고 hash를 확인했지만 이 첫 D434 저장을 105 WI 처리나 지식/벌목/수확의 실제 기능으로 세지 않는다. Scene/Play/Save/E 승격/r129·야간/commit/push 0.
