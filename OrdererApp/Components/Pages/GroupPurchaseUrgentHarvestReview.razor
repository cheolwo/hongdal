@page "/group-purchase/urgent-harvest-offers/{SupplyOfferDraftId}/review"

<PageTitle>긴급 수확 연결 검토</PageTitle>

<MudContainer MaxWidth="MaxWidth.Medium" Class="py-4 px-0">
    <같이주문탐색여정Screen 단계="같이주문여정단계.긴급수확검토"
                            문맥키="@SupplyOfferDraftId" />
</MudContainer>

@code {
    [Parameter] public string SupplyOfferDraftId { get; set; } = string.Empty;
}
