@page "/community/decorations/checkout/{ProductKey}"

<CommunityDecorationCheckoutScreen ProductKey="@ProductKey" />

@code {
    [Parameter]
    public string ProductKey { get; set; } = string.Empty;
}
