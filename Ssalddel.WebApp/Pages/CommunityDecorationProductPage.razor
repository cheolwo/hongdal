@page "/community/decorations/products/{ProductKey}"

<CommunityDecorationProductScreen ProductKey="@ProductKey" />

@code {
    [Parameter]
    public string ProductKey { get; set; } = string.Empty;
}
